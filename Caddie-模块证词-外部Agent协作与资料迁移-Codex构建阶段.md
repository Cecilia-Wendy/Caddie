# Caddie 模块构建证词：外部 Agent 协作与资料迁移

> 证词提交者：Codex  
> 参与阶段：2026 年 7 月下旬，Caddie for Agent、Harness 与 Alpha 基础设施收口阶段  
> 证词范围：外部 Agent 接入、安全读写与回流、连接反馈、岗位及面试事务候选修改、Agent 辅助资料迁移向导  
> 写作原则：只记录我亲自参与、实际查看或能由仓库文件、测试、Git 记录支持的内容。  
> 非证词范围：我不代表整个 Caddie 产品，也不对面试 V2/V3、机会库、内置专家效果、商业化或真实用户规模作全局判断。

---

## 证词边界

### 我负责或深度参与过的模块

1. 【事实】外部 Agent 操作 Caddie 的本地协议与工具入口。
   - MCP 入口：`caddie_mcp.py`
   - CLI 入口：`caddie_agent_cli.py`
   - 本地 REST API：`server.py` 中 `/api/agent/*`
   - 启动脚本：`scripts/caddie-agent.sh`、`scripts/caddie-mcp.sh`

2. 【事实】外部 Agent 的受控写入与用户确认机制。
   - 读取工作区、岗位、项目、文档和增量变更；
   - 保存低风险草稿；
   - 对事实、反馈、正式文档、岗位事务和面试排期只提交候选；
   - 使用基准版本或哈希防止旧候选覆盖新内容。

3. 【事实】外部 Agent 的连接配置、真实握手反馈与接入页面。
   - Codex、Claude Desktop、WorkBuddy、本地 CLI；
   - 一键写入并备份 MCP 配置；
   - 区分“未配置、等待验证、连接正常”；
   - 真实调用后写入 `~/.caddie/agent-connections.json`。

4. 【事实】Agent 辅助资料迁移向导。
   - 入口位于“设置 → 外部 Agent”；
   - 用户选择 Agent、授权目录和迁移类型；
   - Caddie 生成受控迁移任务；
   - 流程被明确拆成“盘点、确认结构、分批迁移、验收报告”；
   - 迁移任务通过 `agent_tasks` 和 `agent_events` 记录。

5. 【事实】岗位与面试排期的外部 Agent 候选修改。
   - `propose_job_record_update`
   - `propose_interview_change`
   - 服务端确认后才执行 `update_job_record` 或 `change_interview_schedule`
   - 我还实际定位并修正过一条错误挂载的国泰海通面试记录，但这是一次数据纠错案例，不是模块本身的普遍效果证明。

### 大致参与时间或阶段

- 【事实】仓库提交 `dbfe1fa` 的时间是 2026-07-27 02:31:43，提交信息为“完善外部 Agent 协作与 Caddie Alpha 基础设施”。
- 【事实】仓库提交 `376856f` 的时间是 2026-07-27 19:46:24，提交信息为“checkpoint Caddie agent workspace redesign”，其中包含之后的迁移向导和同期工作区改动。
- 【判断】我参与的是 Caddie 已经有岗位、项目、文档、Agent Runtime 和本地扫描之后的“对外开放与安全回流”阶段，不是产品从零搭建阶段。

### 我直接修改或查看过的对象

#### 直接修改或深度参与

- `caddie_mcp.py`
- `caddie_agent_cli.py`
- `server.py`
- `db.py`
- `context.py`
- `static/index.html`
- `static/caddie-ui.css`
- `tests/test_external_agent_protocol.py`
- `tests/test_mcp_smoke.py`
- `scripts/caddie-agent.sh`
- `scripts/caddie-mcp.sh`
- `docs/EPIC-Agent可操作的求职资产基础设施.md`
- `docs/agent-protocol/01-DATA-CONTRACT.md`
- `docs/agent-protocol/02-ACTION-CONTRACT.md`
- `docs/agent-protocol/03-APPROVAL-POLICY.md`
- `docs/agent-protocol/04-ACCEPTANCE-SCENARIOS.md`
- `docs/agent-protocol/05-CURRENT-STATE-GAP-AUDIT.md`
- `docs/agent-protocol/06-PROVISIONAL-UX-DECISIONS.md`

#### 直接查看并作为依赖使用，但不认领为主要成果

- `agent_runtime.py`
- `harness.py`
- `retrieval.py`
- `interview_store.py`
- `docs/HARNESS-执行框架.md`
- `agent_tasks`、`agent_runs`、`agent_events`、`proposed_changes`
- `career_facts`、`workspace_change_events`
- `local_discovery_runs`、`local_file_candidates`
- `assets`、`sources`、`knowledge_items`、`job_tracks`、`applications`、`interviews`

### 我亲自知道、推测与未知的边界

- 【事实】我亲自知道代码中提供了哪些工具、哪些写入需要确认、连接状态如何记录、迁移任务如何生成，以及测试覆盖了哪些协议路径。
- 【事实】我亲自在本地浏览器看到“设置 → 外部 Agent”中的迁移入口，看到 Codex 和 WorkBuddy 的真实连接状态，并打开过迁移表单；当时浏览器控制台没有错误。
- 【事实】我亲自运行过 Python 导入、数据库初始化、前端 JavaScript 语法检查和 `/api/agent/migrations` 接口检查。
- 【判断】这些基础设施降低了外部 Agent 与 Caddie 协作时的误写风险和用户配置成本。
- 【假设】普通用户愿意把已有 Agent 当作迁移工具，并愿意回到 Caddie 处理候选。
- 【未知】目前有多少真实用户完成过一次端到端批量迁移；迁移向导是否显著提高激活率或留存；内置 Agent 与外部 Agent 的效果差异。

---

## 1. 我接到的原始任务

### 1.1 用户最初要求解决什么问题

【事实】这一阶段的任务不是“再做一个聊天框”，而是让 Caddie 可以成为 Codex、Claude、WorkBuddy 等 Agent 能操作的本地职业工作区。

用户表达过三个使用场景：

1. Caddie 只作为结构化职业资料工具，由用户手动整理；
2. 用户使用 Caddie 自带 AI/Agent 完成求职任务；
3. 用户把 Codex 等成熟外部 Agent 作为主操作台，让外部 Agent 读取 Caddie、执行任务并把结果回流。

随后用户进一步提出两个直接问题：

- 现有 Agent 如何顺利操控 Caddie，而不是只能生成一份孤立草稿；
- 新用户已有大量本地文件，如何让 Agent 协助迁移，减少手工建档成本。

### 1.2 用户提供的背景、案例或材料

【事实】用户提供了以下真实背景：

- 自己长期在 Codex、Claude 等对话中准备求职，重要内容散落在本地文档和聊天中；
- Caddie 已有岗位、项目、面试准备、知识和收件箱等页面；
- 用户不一定能为 Caddie 接入最强模型，但已经在使用打包好的 Codex、Claude 或 WorkBuddy；
- 用户希望 Agent 可以直接读取授权的本地文件，不必先把全部资料导入收件箱；
- 用户展示了外部 Agent 把结果保存为 `asset` 后，在岗位“准备知识”页面看不到的失败案例；
- 用户展示了 Agent 无法修改岗位面试排期，因为 MCP 权限范围不够的失败案例；
- 用户展示了接入页只有“重新检测”，但没有清楚说明是否真正接入成功的交互问题；
- 用户展示了本地扫描路径手输、扫描时间长、结果入口不清楚等迁移摩擦。

### 1.3 我最开始如何理解需求

【判断】我最初把问题理解为“给 Caddie 增加一套可供外部 Agent 调用的接口”，重点放在：

- 读取上下文；
- 保存草稿；
- 提交文档候选；
- 用任务、运行、事件记录过程；
- 使用 MCP 和 CLI 兼容不同 Agent。

这个理解在工程上是成立的，但后来证明不够。它解决了“Agent 能否调用”，没有完整解决：

- Agent 应该写到哪个用户可见位置；
- 用户如何知道接入是否真实成功；
- 岗位状态、面试排期等非文档事务如何修改；
- 大规模资料迁移如何分阶段、可暂停、可确认；
- 用户如何不用理解 ID、表名和工具名。

### 1.4 我最初提出或实现的方案

【事实】第一版方案包括：

- `workspace_bootstrap`：告诉 Agent 能力、规则和权限；
- `read_context_package`：按岗位、项目和文档读取最小上下文；
- `get_workspace_map`：解析稳定 ID 和可写目录；
- `search_caddie`、`read_document`、`get_changes_since`：检索、读取和续接；
- `create_draft_asset`：保存低风险草稿；
- `propose_fact`、`propose_feedback`、`propose_document_change`：提交待确认候选；
- `start_external_run`、`append_task_event`、`complete_external_run`：记录外部 Agent 任务过程；
- MCP、CLI 和 REST API 共用同一数据边界。

【判断】这套方案建立了操作边界，但当时仍偏“工具清单”，没有充分表达用户任务和最终落点。

---

## 2. 用户如何纠正我

### 2.1 从“外部 Agent 能写”纠正为“必须写到用户正在看的对象里”

- **你的原方案：** 外部 Agent 生成内容后，可调用 `create_draft_asset` 保存一份草稿。
- **用户为什么不满意：** 用户在字节 PMO 岗位的“准备知识”页看不到内容。草稿存在数据库里，但没有进入该页面实际读取的文档体系。技术上保存成功，产品上等于没有完成任务。
- **用户实际想要的：** 用户说“把内容更新到这个岗位的面试准备里”，结果就必须出现在该岗位的正确文件夹和页面，而不是某个抽象资产库。
- **最终如何修改：** 增加 `get_workspace_map` 暴露稳定写入位置；增加 `save_career_document`，要求明确 `destination`；岗位准备内容使用 `propose_track_knowledge_document`，确认后进入对应知识文件夹。
- **这次修改反映了什么产品判断：** Agent 动作的完成标准不是“API 返回 200”，而是用户能在预期对象、预期页面、预期状态下找到结果。
- **证据位置：** `caddie_mcp.py` 中 `get_workspace_map`、`save_career_document`；`docs/agent-protocol/06-PROVISIONAL-UX-DECISIONS.md` 的 U4；本轮对话中“这个没有看到”的截图与反馈。

### 2.2 从“给用户快捷命令”纠正为“用户不应该学习工具协议”

- **你的原方案：** 提供 CLI 命令、工具名、岗位 ID、项目 ID 和建议快捷指令，帮助用户手动驱动 Agent。
- **用户为什么不满意：** 用户连续反馈“这是啥意思，我没看懂”“没啥用啊”。这些设计把系统内部概念暴露给了用户。
- **用户实际想要的：** 用户只表达“把国泰海通面试时间改对”“把面试准备写进这个岗位”“把本地资料迁进来”，系统应自行解析对象和动作。
- **最终如何修改：** 增加 `get_workspace_map`；设置页改成“一键接入、复制验证指令、开始迁移”；迁移向导自动生成完整任务合同，不要求用户自己写 Prompt 或查 ID。
- **这次修改反映了什么产品判断：** Agent 产品的自然语言入口不等于把 API 文档翻译成中文。用户意图和内部动作之间仍需要产品层做对象解析、权限判断和状态反馈。
- **证据位置：** `static/index.html` 的外部 Agent 接入和迁移向导；`server.py` 的 `_migration_task_prompt`；本轮对话中的连续否定。

### 2.3 从“配置已写入”纠正为“必须证明真实连接”

- **你的原方案：** 检查 Codex、Claude、WorkBuddy 的配置文件中是否存在 Caddie，即显示“已接入”。
- **用户为什么不满意：** 用户指出页面没有反馈，无法知道客户端是否真的启动 MCP 并成功调用 Caddie。配置存在不等于连接可用。
- **用户实际想要的：** 明确区分未配置、已写配置但未调用、已完成真实握手。
- **最终如何修改：** 增加 `verify_connection`；MCP 启动和验证时写入 `~/.caddie/agent-connections.json`；前端显示“未配置、等待验证、连接正常”，以及最近连接时间、协议版本和工具数量。
- **这次修改反映了什么产品判断：** 接入状态必须来自真实行为证据，而不是静态配置推断。用户需要可观察性，而不是系统自我宣称。
- **证据位置：** `caddie_mcp.py` 的 `_record_connection`、`verify_connection`；`server.py` 的 `_agent_connection_registry`；`static/index.html` 的 `loadAgentIntegrations`；用户“我怎么知道有没有接入成功”的截图。

### 2.4 从“只允许内容写入”纠正为“Agent 还要处理职业事务”

- **你的原方案：** 外部 Agent 只能提交文档、事实、反馈和草稿。
- **用户为什么不满意：** 用户要求 Agent 修正错误的面试日历，外部 Agent 返回“没有工具能改岗位排期字段”。这暴露了权限面过窄。
- **用户实际想要的：** Agent 可以提议更新岗位状态、下一步行动、投递日期和面试排期，但不能绕过用户确认。
- **最终如何修改：** 增加 `propose_job_record_update` 与 `propose_interview_change`；服务端支持 `update_job_record` 和 `change_interview_schedule` 的候选应用，并做对象和版本检查。
- **这次修改反映了什么产品判断：** Caddie 的对象不仅是内容资产，还包括真实求职事件。Agent 若只能写文章，无法覆盖完整任务；但事务修改风险更高，必须进入候选确认。
- **证据位置：** `caddie_mcp.py` 对应工具；`server.py` 候选应用逻辑；`tests/test_external_agent_protocol.py`；用户关于“权限开的好像不太够”的原话。

### 2.5 从“手输本地路径”纠正为“使用系统文件夹选择器”

- **你的原方案：** 在扫描弹窗中提供多行路径输入框和常用目录勾选。
- **用户为什么不满意：** 用户认为路径难写、摩擦大，应该直接打开本地目录选择。
- **用户实际想要的：** 使用原生文件夹选择器，选择具体目录后再扫描。
- **最终如何修改：** 增加 `/api/local-discovery/pick-folder`；macOS 使用 `osascript choose folder`；前端提供“选择文件夹”，并显示已选目录。
- **这次修改反映了什么产品判断：** 本地优先产品不能把文件系统路径语法作为用户门槛。授权动作应可见、具体且由用户主动触发。
- **证据位置：** `server.py` 的 `pick_local_discovery_folder`；`static/index.html` 的 `pickDiscoveryFolder`；用户对路径输入框的截图。

### 2.6 从“扫描时顺便 AI 识别”纠正为“先快扫，再按需使用 AI”

- **你的原方案：** 扫描本地目录时可以开启 AI 复核高置信候选。
- **用户为什么不满意：** 用户反馈扫描等待时间太长，界面只显示笼统的“正在扫描”。
- **用户实际想要的：** 文件发现应先快速完成；AI 深度识别不是扫描的前置条件；过程应可观察。
- **最终如何修改：** 本地扫描先使用规则、哈希和样本文本生成候选；AI 复核变为可选并限制数量；扫描任务记录进度并支持查询和取消。
- **这次修改反映了什么产品判断：** AI 不应阻塞确定性任务。可由文件系统、哈希和规则完成的部分应先完成，模型只处理需要语义判断的候选。
- **证据位置：** `db.py` 的 `local_discovery_runs`、`local_file_candidates`；`server.py` 的 `_perform_local_discovery_scan`；`docs/HARNESS-执行框架.md` 的 Local Context Harness。

### 2.7 从“收件箱是所有入口”纠正为“Agent 可直接读取授权文件完成任务”

- **你的原方案：** 扫描到的文件先进入收件箱，用户导入后 Agent 才能使用。
- **用户为什么不满意：** 用户明确指出，Caddie AI 应像 Codex 一样直接读取本地文件执行任务，不应强迫用户先完成资料入库。
- **用户实际想要的：** 两条路径并存：
  - 临时读取：用户授权本地文件，本轮任务直接使用；
  - 长期入库：需要沉淀时再进入收件箱和确认流程。
- **最终如何修改：** Harness 文档明确“收件箱 = 迁移/入库，问 Caddie = 临时读取/执行任务”；验收场景要求外部 Agent 可读取已授权候选文件，不必全部导入。
- **这次修改反映了什么产品判断：** 读取权和长期写入权不应绑定。降低首次使用成本，不能以牺牲长期资料质量为代价。
- **证据位置：** `docs/HARNESS-执行框架.md`；`docs/agent-protocol/04-ACCEPTANCE-SCENARIOS.md` 的 S5；用户“其实不应该放在收件箱，而是放在问 Caddie”的原话。

### 2.8 从“简单迁移入口”纠正为“完整迁移任务”

- **你的原方案：** 提供上传、粘贴、本地扫描等入口，或者给用户一段通用 Agent 指令。
- **用户为什么不满意：** 用户认为要做就应做全面，避免后续反复修改；迁移不是把文件塞进系统，而是要处理分类、重复版本、归属、冲突和确认。
- **用户实际想要的：** 使用已有 Agent 完成盘点和结构建议，用户确认后分批迁移，并能暂停、续接和验收。
- **最终如何修改：** 新增 Agent 迁移任务：
  - 选择已验证 Agent；
  - 选择授权目录；
  - 选择迁移类型；
  - 创建 `data_migration` 任务；
  - 自动生成分阶段任务合同；
  - 每批最多 10 项；
  - 使用 `append_task_event` 更新进度；
  - 最终输出扫描数、迁入数、重复数、跳过数、冲突数和待人工处理项。
- **这次修改反映了什么产品判断：** 数据迁移是建立信任和结构的过程，不是一次批量上传。首次建库如果污染事实，后续所有 AI 结果都会受影响。
- **证据位置：** `server.py` 的 `AgentMigrationStartIn`、`_migration_task_prompt`、`/api/agent/migrations`；`static/index.html` 的 `openAgentMigration`；`static/caddie-ui.css` 的迁移区样式。

### 2.9 用户坚持 AI 不能编造或擅自修改

- **你的原方案：** 一部分早期设计倾向于让 Agent 自动保存结果，减少确认步骤。
- **用户为什么不满意：** 用户多次强调事实、数字、个人贡献、正式文档和面试安排不能由 AI 自行决定；用户编辑必须优先。
- **用户实际想要的：** AI 可以提出、整理、比较和生成草稿，但“这是不是我的事实”“是否覆盖正式版本”“是否改变日历和流程”由人决定。
- **最终如何修改：** 风险分层：
  - 低风险草稿可以保存，但必须带 provenance；
  - 事实、反馈、文档覆盖、岗位记录、面试排期进入 `proposed_changes`；
  - 使用基准哈希或版本检查；
  - 用户修改后的内容成为最终版本；
  - Agent 通过 `get_changes_since` 读取用户确认结果。
- **这次修改反映了什么产品判断：** 对 AI 不确定性的正确处理不是要求模型“更谨慎”，而是把不确定性变成候选、证据、版本和确认步骤。
- **证据位置：** `docs/agent-protocol/03-APPROVAL-POLICY.md`、`06-PROVISIONAL-UX-DECISIONS.md`；`proposed_changes`、`career_facts`、`workspace_change_events`；`tests/test_external_agent_protocol.py`。

---

## 3. 模块解决的真实问题

### 3.1 服务的具体用户任务

【事实】该模块服务的不是泛化聊天，而是以下任务：

> 用户已经在 Codex、Claude 或 WorkBuddy 中完成求职分析或准备，希望 Agent 能读取 Caddie 中正确的岗位和项目上下文，把产出写回正确位置，并在涉及事实和事务时交给用户确认。

迁移子任务是：

> 用户已有大量本地简历、项目、JD 和面试文件，希望复用现有 Agent 盘点并迁入 Caddie，而不是从空白表单重新建档。

### 3.2 没有模块时的原有做法

【事实】根据用户实际操作记录，原来通常是：

1. 在 Codex 或 Claude 中聊天；
2. 手动复制 Caddie 的项目资料或上传本地文件；
3. 得到一段回答或文档；
4. 用户自己判断应该建什么文件；
5. 手动复制到 Caddie；
6. 再手动更新岗位状态、面试时间或反馈；
7. 下一次对话继续重新解释背景。

### 3.3 原方法最困难的地方

【判断】困难不只是复制粘贴耗时，而是四类认知负担：

- **对象归属：** 这段内容属于岗位、项目、简历版本、知识还是反馈；
- **事实边界：** 哪些是原始事实，哪些是 Agent 推断或表达优化；
- **版本冲突：** Agent 使用的是不是最新文档，是否会覆盖用户后来修改；
- **流程续接：** Agent 完成后，Caddie 是否知道发生了什么；用户确认后，下一次 Agent 是否知道最终结果。

### 3.4 改变的是效率、质量、认知、决策还是行为

- 【事实】**效率：** 减少查 ID、复制上下文、手工创建文件和重复录入。
- 【判断】**质量：** 通过最小上下文、反馈约束、证据和版本检查，降低跨岗位混淆与旧内容覆盖。
- 【判断】**认知：** 把“AI 回答”拆成草稿、事实候选、反馈候选、文档候选和外部事务，让用户知道每类结果的风险不同。
- 【事实】**决策：** 用户仍决定事实是否成立、正式文档是否覆盖、排期是否修改。
- 【判断】**行为：** 用户可以把外部 Agent 作为推理与创作入口，把 Caddie 作为确认、版本和长期资料入口。

### 3.5 使用前后差异

#### 使用前

- 用户必须告诉 Agent Caddie 是什么；
- 用户手工提供背景和文件；
- Agent 输出只存在聊天里；
- 保存成功不等于出现在正确页面；
- 用户不知道接入是否真的可用；
- Agent 无法处理岗位和面试事务；
- 批量迁移容易产生重复和事实污染。

#### 使用后

- Agent 可先调用 bootstrap 和 workspace map；
- Agent 按任务读取最小上下文；
- 内容进入明确 destination；
- 正式修改进入同一候选确认机制；
- 连接状态有真实握手证据；
- 岗位和排期可提交候选；
- 迁移先盘点和去重，再分批确认。

### 3.6 真实使用案例

#### 案例 A：字节 PMO 面试准备写错位置

- 【事实】**输入：** 用户要求外部 Agent 把字节 PMO 三面准备内容更新到 Caddie。
- 【事实】**原操作：** Agent 使用 `create_draft_asset` 保存两份草稿。
- 【事实】**失败输出：** Caddie 首页能看到 Agent 更新，但岗位“准备知识”页面为空。
- 【事实】**后续纠正：** 明确岗位准备文档应使用岗位文件夹写入入口；增加稳定写入目录解析。
- 【判断】**用途：** 该案例证明“数据库写成功”不是用户任务完成，结果必须进入用户预期的信息结构。

#### 案例 B：国泰海通面试时间挂错岗位

- 【事实】**输入：** 用户发现日历中的面试记录对应错了岗位。
- 【事实】**原操作：** 外部 Agent检查 MCP 后表示只能改文档，不能改岗位排期。
- 【事实】**后续实现：** 增加岗位记录与面试安排候选工具；同时我实际核对本地数据，将一条 2026-07-28 09:00 的资本市场面试从错误的股权业务 application 移到资本市场 application。
- 【事实】**输出：** 股权业务只保留原有面试；资本市场岗位获得正确的下一场面试。
- 【判断】**用途：** 说明 Caddie 中有一类核心对象是职业事件，不是内容文档。

#### 案例 C：外部 Agent 接入验证

- 【事实】**输入：** 用户在接入页看到“已接入”，但不知道是否真的可用。
- 【事实】**操作：** Agent 调用 `verify_connection`，写入本地连接回执；页面重新检测。
- 【事实】**输出：** 页面显示 Codex 或 WorkBuddy 为“连接正常”，并显示协议版本、工具数和最近连接时间。
- 【判断】**用途：** 这是接入可观察性案例，不是求职结果案例。

#### 案例 D：Agent 资料迁移向导

- 【事实】**输入：** 用户希望新用户优先使用已有 Agent 上传和迁移资料。
- 【事实】**操作：** 在设置页选择已验证 Agent、资料文件夹和迁移类型，Caddie 创建 `data_migration` 任务并生成任务合同。
- 【事实】**输出：** 任务要求 Agent 先盘点、不写库；用户确认结构后分批提交；最后生成迁移报告。
- 【事实】**验证状态：** 页面、表单、API 和任务生成逻辑已实现并通过本地检查。
- 【未知】**后续用途证据：** 尚没有我亲自完成并观察的真实大批量迁移结果，因此不能声称该流程已经被用户验证。

---

## 4. AI 在其中的作用

### 必须由 AI 完成

#### 4.1 异构资料的语义识别与结构建议

- 【判断】AI 需要判断一份文件更像简历、项目证据、JD、面试反馈还是知识资料，并推测它可能关联哪个岗位或项目。
- 【判断】固定规则可以识别扩展名、时间、哈希和部分关键词，但无法可靠理解“同一项目的复盘稿”和“该项目的正式介绍稿”之间的语义关系。
- 【判断】输出会随用户的公司、项目、岗位和历史版本变化。
- 【判断】错误会导致错误归属、重复项目或把旧版本当成当前事实，因此结果只能是候选。

#### 4.2 从具体材料中提取候选事实与冲突

- 【判断】AI 需要理解数字的口径、时间范围、动作与结果关系、个人和团队贡献边界。
- 【判断】固定规则无法仅凭句式判断“参与”“主导”“独立负责”的真实含义。
- 【判断】不同材料可能对同一结果有不同表述，AI 需要提出冲突而不是自动选择。
- 【判断】错误会长期污染简历、面试话术和后续 Agent 上下文，所以必须展示证据并由用户确认。

#### 4.3 迁移结构方案

- 【判断】AI 可以根据用户已有资料和 Caddie 当前对象，建议“合并为一个项目”“保留为两个简历版本”“关联到已有岗位而非新建岗位”。
- 【判断】这是跨文件、跨对象的语义判断，无法仅靠文件夹层级完成。
- 【判断】但最终结构仍不能由 AI 单方面决定。

### AI 可以增强，但并非必要

#### 4.4 文件分类复核

- 不用 AI：可按路径、扩展名、文件名关键词、哈希和规则生成候选。
- AI 增强：改善标题、摘要、类型和关联建议。
- 【事实】当前本地扫描已经把 AI 复核设置为可选，而不是前置条件。

#### 4.5 迁移任务说明生成

- 不用 AI：Caddie 可以用固定模板生成任务合同，当前实现就是确定性模板。
- AI 增强：未来可以根据资料规模和用户目标调整批次策略。
- 【事实】目前 `_migration_task_prompt` 是普通软件生成的模板，不应包装成 AI 能力。

#### 4.6 输出摘要与迁移报告

- 不用 AI：数量、状态、重复数和冲突数可由数据库规则统计。
- AI 增强：解释哪些冲突最重要、下一步优先处理什么。

### 普通软件能力

以下均不应包装成 AI 亮点：

- MCP/CLI/REST 接入；
- 配置文件备份与写入；
- 连接回执和最近连接时间；
- Agent 身份；
- `agent_tasks`、`agent_runs`、`agent_events`；
- 任务状态和进度；
- 文件夹选择器；
- 文件遍历、扩展名筛选、大小限制；
- 内容哈希与重复识别；
- 版本号、基准哈希和冲突保护；
- `proposed_changes`；
- 用户确认、拒绝和修改；
- Git 版本提交；
- 页面展示、筛选、状态标签；
- 岗位 ID、项目 ID 和 destination 解析。

---

## 5. 人与 AI 如何共同工作

### 5.1 AI 提出什么

【事实】在本模块的设计中，AI 可以提出：

- 文件类型和归属建议；
- 迁移结构方案；
- 新事实和证据；
- 反馈约束；
- 文档草稿或正式文档修改候选；
- 岗位记录修改候选；
- 面试排期新增、修改或删除候选；
- 重复、冲突和需要人工核验的项目。

### 5.2 用户在哪一步判断、修改或确认

【事实】用户至少在以下节点保留判断权：

1. 选择允许读取的目录；
2. 确认迁移结构；
3. 确认每批内容；
4. 确认事实、数字和贡献边界；
5. 修改 Agent 的文档候选；
6. 确认正式文档覆盖；
7. 确认岗位状态和面试排期；
8. 决定冲突材料以哪个版本为准。

### 5.3 用户纠正是否影响后续结果

【事实】设计上会影响：

- 用户确认后的最终事实进入 `career_facts`；
- 用户反馈进入 `feedback_notes`；
- 用户修改后的文档成为新版本；
- 工作区变化写入 `workspace_change_events`；
- 后续 Agent 使用 `get_changes_since` 获取确认结果和变更游标；
- 文档候选使用基准哈希，用户中途修改后，旧候选不能静默覆盖。

【未知】迁移向导产生的用户纠正是否已经在大量真实任务中验证。

### 5.4 影响如何发生

【事实】影响不是依赖模型“记住用户说过什么”，而是通过：

- 结构化事实；
- 反馈 directive；
- 对象版本；
- 工作区变更事件；
- 上下文装配；
- 写入前冲突检查。

### 5.5 用户不参与判断，产品是否仍成立

【判断】作为只读资料管理和临时问答工具仍然成立；作为长期可信职业资产库则不成立。

原因不是 AI 一定会错，而是以下判断无法外包：

- 某个数字是否采用这个口径；
- 某项工作是否属于个人贡献；
- 哪个简历版本代表当前自我叙事；
- 某条面试时间是否真实；
- 某个反馈是否应成为全局约束。

### 5.6 体现的人机分工

【判断】该模块的人机分工是：

- AI 负责扩大阅读范围、发现关系、生成候选、解释差异；
- 软件负责权限、状态、证据、版本、目标对象和冲突；
- 人负责事实成立、贡献边界、正式版本和现实事务。

这不是“AI 生成，人点确认”这么简单。关键是系统把不同风险的结果拆开，让用户知道自己在确认什么。

---

## 6. 与 Skill 或通用对话的关系

### 6.1 可以直接写成 Skill 的能力

【判断】以下能力完全可以写成 Skill：

- 介绍 Caddie 的操作规则；
- 规定先 bootstrap、再读取最小上下文；
- 规定输出结构；
- 规定不得编造数字或贡献；
- 规定结束时给出回流摘要；
- 规定迁移先盘点、再规划、分批处理；
- 指导 Agent 选择何种工具。

实际上，迁移任务合同和 Harness 中相当一部分就是可执行的 Skill/Prompt。

### 6.2 用 ChatGPT 或 Claude 对话就能完成的能力

【判断】以下单次任务不需要独立产品：

- 修改一段简历；
- 分析一个 JD；
- 生成自我介绍；
- 对一份面试逐字稿做复盘；
- 根据一批文件给出分类建议；
- 输出一次迁移清单；
- 给出岗位准备计划。

用户愿意手动上传材料、自己维护结果时，通用对话已经足够。

### 6.3 只保留 Skill 会明显退化的任务

#### 任务一：结果写入正确对象

退化点：Skill 能要求“保存到字节 PMO 面试准备”，但无法知道 Caddie 当前真实的岗位 ID、文件夹 ID、文档版本和页面读取结构，也无法证明保存后用户能看到。

#### 任务二：旧候选与新版本冲突

退化点：Skill 可以提醒“不要覆盖”，但无法原子地比较基准哈希、拒绝旧候选、记录用户最终版本。

#### 任务三：用户确认后的后续续接

退化点：Skill 无法稳定提供变更游标、确认状态和结构化反馈；下一次对话仍可能依赖用户复述。

#### 任务四：现实求职事务

退化点：Skill 可以生成“建议更新面试时间”，但不能将其作为带证据、可确认、可回滚的岗位事件。

#### 任务五：大规模迁移

退化点：Skill 可以扫描和输出清单，但缺少持久批次、去重索引、任务状态、用户逐批确认和失败续接。

### 6.4 独立产品形态是否必要

【判断】对“完成一次求职建议”不是必要；对“维护一个长期、可被多人和多个 Agent 操作的职业事实工作区”有必要。

具体证据不是抽象的“体验更好”，而是：

- `get_workspace_map` 提供真实对象和写入位置；
- `proposed_changes` 承接不同 Agent 的候选；
- 基准哈希阻止旧候选覆盖；
- `workspace_change_events` 让后续 Agent 读取用户最终决定；
- 岗位与面试事务具有真实状态；
- 同一结果能出现在岗位页面、版本和日历中，而不是只留在聊天里。

【判断】如果 Caddie 最终只做“上传文件后让 AI 回答”，独立产品没有充分必要性，一个文件系统 Skill 就能替代大部分价值。

---

## 7. 模块在整个产品中的位置

### 7.1 位置选择

**结论：支撑基础设施；核心闭环的一部分；Agent 资料迁移向导属于已实现但尚未验证。**

- 【事实】外部 Agent 协作不是简历、面试或岗位本身；
- 【判断】它让这些已有对象可以被外部 Agent 安全读取和回流；
- 【事实】迁移向导已经实现页面和 API；
- 【未知】尚无端到端大规模迁移使用证据。

### 7.2 删除后核心用户价值是否成立

【判断】仍然成立，但使用方式退化。

删除外部 Agent 基础设施后，用户仍可：

- 手动管理经历、岗位、文档和面试；
- 使用 Caddie 内置 AI；
- 手动上传和粘贴资料；
- 手动复制外部 Agent 结果。

因此这些能力解决的首先是“让 Agent 安全操作产品”和“降低迁移成本”，不是职业资料管理本身的唯一核心。

### 7.3 上游依赖

【事实】依赖：

- 经历和项目对象；
- `job_tracks`、`applications`、`interviews`；
- 知识文件夹和知识文档；
- `assets`、`sources`；
- 上下文装配和检索；
- Agent Runtime；
- 反馈约束；
- 本地版本与 Git 提交。

### 7.4 下游使用

【事实】输出可被以下下游使用：

- 岗位准备知识页面；
- 项目文档；
- 草稿资产；
- 事实和反馈；
- 岗位工作台；
- 面试日历；
- Agent 更新包；
- 后续 Agent 的上下文读取。

### 7.5 是否把真实反馈传回上游

【事实】协议和代码支持用户确认、修改、拒绝后写入变更事件，并由 Agent 读取增量。

【判断】这是闭环设计。

【未知】目前是否已经形成稳定的真实用户行为闭环和可量化改进。

---

## 8. 证据与成熟度

### 8.1 相关文件路径

#### 主要实现

- `caddie_mcp.py`
- `caddie_agent_cli.py`
- `server.py`
- `db.py`
- `context.py`
- `static/index.html`
- `static/caddie-ui.css`
- `scripts/caddie-agent.sh`
- `scripts/caddie-mcp.sh`

#### 测试

- `tests/test_external_agent_protocol.py`
- `tests/test_mcp_smoke.py`
- `tests/test_agent_project_update.py`
- `tests/test_agent_interview_context.py`
- `tests/test_profile_change_preflight.py`
- `tests/test_profile_write_targets.py`

#### 设计与验收

- `docs/EPIC-Agent可操作的求职资产基础设施.md`
- `docs/HARNESS-执行框架.md`
- `docs/agent-protocol/01-DATA-CONTRACT.md`
- `docs/agent-protocol/02-ACTION-CONTRACT.md`
- `docs/agent-protocol/03-APPROVAL-POLICY.md`
- `docs/agent-protocol/04-ACCEPTANCE-SCENARIOS.md`
- `docs/agent-protocol/05-CURRENT-STATE-GAP-AUDIT.md`
- `docs/agent-protocol/06-PROVISIONAL-UX-DECISIONS.md`

### 8.2 关键代码与数据表

#### 关键工具

- `verify_connection`
- `workspace_bootstrap`
- `get_workspace_map`
- `read_context_package`
- `search_caddie`
- `read_document`
- `get_changes_since`
- `save_career_document`
- `create_draft_asset`
- `propose_fact`
- `propose_feedback`
- `propose_document_change`
- `propose_track_knowledge_document`
- `propose_job_record_update`
- `propose_interview_change`
- `append_task_event`

#### 关键表

- `agent_tasks`
- `agent_runs`
- `agent_events`
- `proposed_changes`
- `career_facts`
- `workspace_change_events`
- `local_discovery_runs`
- `local_file_candidates`

### 8.3 对应页面

- “设置 → 外部 Agent”
- “设置 → 外部 Agent → 推荐迁移方式”
- 首次引导第 1 步“让已有 Agent 帮我迁移”
- 首页“Agent 更新”
- 岗位详情“准备知识”
- 本地资料发现弹窗

### 8.4 Git 提交记录

- 【事实】`dbfe1fa`：`完善外部 Agent 协作与 Caddie Alpha 基础设施`
- 【事实】`376856f`：`checkpoint Caddie agent workspace redesign`
- 【限制】这两个提交都混合了其他模块改动，不能把提交中的全部文件和全部功能认领为本模块成果。

### 8.5 用户原话或需求记录

以下来自构建对话，未单独存为产品研究访谈：

- 【事实】“它作为一个 Codex 这样的 Agent 的一个可以使用的工具。”
- 【事实】“不仅是 Codex 能用，WorkBuddy、Claude Code 这些都能够直接操作 Caddie。”
- 【事实】“不用把资料先放到收件箱，而是可以直接本地读取，然后执行任务。”
- 【事实】“我觉得可以打开本地目录进行文件夹的选择，而不是进行这样的输入。”
- 【事实】“这个扫描时间是不是太久了。”
- 【事实】“你这个也没什么反馈，我怎么知道有没有接入成功。”
- 【事实】“你这个权限开的好像不太够。”
- 【事实】“为了方便用户进行数据迁移，我觉得可以建议优先使用用户已有的 Agent。”

### 8.6 真实数据

- 【事实】连接页曾显示 Codex 和 WorkBuddy 已完成真实握手，并带最近连接时间与工具数量。
- 【事实】本地数据库中存在真实岗位、项目、面试和 Agent 更新记录。
- 【事实】实际修正过国泰海通资本市场岗位 2026-07-28 09:00 的面试归属。
- 【未知】没有我能引用的迁移完成率、节省时间、错误率或用户留存数据。

### 8.7 已完成但没有使用证据的能力

- 【事实】Agent 迁移任务创建、目录选择、类型选择、固定任务合同、任务继续入口已经实现。
- 【事实】迁移接口返回正常，前端表单无控制台错误。
- 【未知】尚未观察到用户完成一次完整的“盘点 → 确认 → 多批迁移 → 报告”。
- 【事实】Claude Desktop 一键配置存在。
- 【未知】我没有亲自验证 Claude Desktop 的完整真实握手。

### 8.8 仍停留在设想中的能力

- 【假设】同一简历多个版本自动形成可靠版本链；
- 【假设】Agent 自动生成可供用户快速确认的迁移结构图；
- 【假设】迁移任务真正支持暂停、恢复和跨客户端续接；
- 【假设】候选按 10 项分批的规则能改善确认质量；
- 【假设】用户愿意回到 Caddie 集中处理 Agent 更新包；
- 【假设】内置 Agent 与外部 Agent 已在所有任务上实现同等 Harness 和同一确认体验；
- 【未知】资料迁移是否应成为首次启动默认入口，还是只适合重度用户。

### 8.9 成熟度结论

- 【事实】外部 Agent 读取、草稿、事实/反馈/文档候选、岗位/面试事务候选、连接验证已有代码和测试。
- 【判断】外部 Agent 安全协作基础设施处于“可用但仍需真实任务收口”的阶段。
- 【事实】资料迁移向导处于“已实现、已做技术与页面验证”的阶段。
- 【未知】资料迁移向导的产品效果尚未验证。

---

## 额外重点结论

### 这些能力解决的是核心问题，还是让 Agent 安全操作产品

【判断】主要解决“让 Agent 安全操作产品”和“降低已有资料迁移成本”。

它们不直接替用户找到工作，也不直接证明 Caddie 的求职辅导质量。但如果 Caddie 选择“人、内置 Agent、外部 Agent 共用同一职业工作区”的产品方向，这些能力是必要基础设施。

### 没有这些基础设施，核心流程能否手动完成

【事实】可以。

用户可以手动上传文件、创建项目、维护岗位、记录面试、复制 Agent 输出。代价是：

- 重复输入；
- 容易放错位置；
- 版本和事实边界依赖用户记忆；
- 多个 Agent 之间难以续接；
- 外部 Agent 无法可靠处理岗位事务。

### 为什么不应该被当成视频开场

【判断】因为 MCP、Harness、候选修改和连接状态都是“系统如何可靠工作”，不是用户最先感受到的求职问题。

视频开场如果先讲：

- 25 个 MCP 工具；
- 多 Agent 接入；
- 权限分层；
- 版本冲突；

用户仍不知道 Caddie替他解决了什么求职困难。这些更适合作为中段“为什么它不只是一个 Skill”的证据。

### 最能体现 AI 不确定性和人类控制权的设计

【判断】最能体现的是：

> 有证据的事实也只进入轻确认；正式文档、贡献边界和现实排期不能因为 Agent 自信就自动生效；用户编辑后的版本优先，旧候选因基准版本冲突被拒绝。

这比“输出后让用户点确认”更完整，因为它同时处理：

- 证据；
- 风险类型；
- 用户修改；
- 版本冲突；
- 后续 Agent 续接。

### 工程完整性与 AI 产品判断的区分

#### 工程完整性

- MCP、CLI、REST 三种入口；
- 配置文件合并和备份；
- 连接回执；
- 数据表和 CRUD；
- 哈希、版本和幂等；
- 任务事件；
- 测试和错误处理；
- 原生文件夹选择器；
- 服务重启与跨平台路径。

#### AI 产品判断

- 默认最小读取，而不是全库注入；
- 临时读取和长期入库分离；
- 草稿与事实分离；
- 有证据的事实仍需确认；
- 贡献边界和数字属于高风险；
- 用户编辑优先；
- 结果必须进入用户预期对象；
- 现实求职事件也属于 Agent 可提议但不可擅改的对象；
- 迁移先盘点和确认结构，再分批执行；
- AI 不阻塞规则能够完成的扫描和去重。

---

## 9. 最重要的结论

### 1. 你参与的模块里，最有价值的产品判断是什么

【判断】Agent 的“写入能力”不应按数据库表或模型能力设计，而应按用户对象、风险和确认责任设计。保存成功不是完成；进入正确对象、保留证据、允许用户修改、能被后续 Agent 读取，才构成闭环。

### 2. 用户做了哪一个你作为 Agent 不会自然做出的决定

【判断】用户坚持把“临时读取本地文件”和“长期导入收件箱”分开。Agent 自然倾向先把资料结构化入库再工作，但用户认为这会制造首次迁移门槛。这个决定让 Caddie 允许用户先完成任务，再决定哪些内容值得沉淀。

### 3. 哪个能力看起来很炫，但不应该成为 Caddie 的主要亮点

【判断】多 Agent、MCP、Harness 和“一键接入 Codex/WorkBuddy”看起来很炫，但不应成为主要亮点。它们证明系统可被可靠操作，不直接证明用户获得了更好的求职判断或面试结果。

### 4. 哪一个真实流程最值得在三分钟 Case Study 中展示

【判断】最值得展示的是“字节 PMO 面试准备回流”：

1. 外部 Agent 读取岗位、滴滴项目和反馈；
2. 生成面试准备；
3. 第一版误存为通用草稿，岗位页看不到；
4. 用户指出完成标准错误；
5. 系统改为解析岗位的真实写入目录；
6. 事实、话术和反馈按风险拆分；
7. 用户确认后进入岗位准备页面；
8. 后续 Agent 能读取最终版本。

这个流程同时展示真实失败、用户纠正、信息架构、AI 不确定性和闭环，不需要用工具数量撑场面。

### 5. 关于 Caddie，你最没有资格下结论的部分是什么

【未知】我最没有资格判断 Caddie 的核心求职辅导质量和真实市场需求是否成立。我参与的是 Agent 如何安全读取、操作和回流，不足以证明：

- Caddie 的内置 Agent 比通用模型更好；
- 求职者会长期使用；
- 面试准备能提高通过率；
- 资料迁移是用户最核心需求；
- Caddie 应该成为独立产品而非高级 Skill。

这些必须由完整产品流程、真实用户行为和结果数据验证。
