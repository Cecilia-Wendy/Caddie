# Caddie 模块构建证词：Agent 知识检索与安全写入

> 证词主体：Codex（检索、上下文装配与安全写入修复阶段）  
> 证词日期：2026-07-27  
> 证词范围：岗位知识检索、对话上下文装配、反馈约束注入、候选修改与确认写入、相关 Agent 工作台交互  
> 不覆盖：Caddie 的完整产品定位、在职日记、邮件接入、简历排版、全部岗位工作台、全部专家 Agent 的产品价值

## 证词边界与参与范围

### 我负责或深度参与过的模块

1. 【事实】修复岗位 Agent 的知识检索错误，重点解决“文档已经存在，但 Agent 因本轮正文没有命中而声称文档不存在”的问题。
2. 【事实】调整岗位对话的上下文装配，使当前岗位的完整知识目录、最近多轮用户意图、标题精确命中结果和生效反馈约束共同进入本轮上下文。
3. 【事实】补充查询归一化，处理用户把 `IPO` 输入为 `i o p` 或 `IOP` 等真实输入变体。
4. 【事实】查看并验证候选写入协议，包括用户确认、用户编辑优先、无证据事实拒绝、旧候选不得覆盖用户新修改。
5. 【事实】查看和调试统一 Agent 工作台中的资料引用、候选修改、写入结果、对话连续性和右侧确认区。
6. 【判断】我参与的是“Agent 能否可靠使用已有资料并安全改变工作区”的基础闭环，不是 Caddie 全部求职业务模块。

### 参与阶段

- 【事实】主要参与阶段是统一 Agent 工作台和外部 Agent 协作基础设施已经存在之后的集中修复阶段，仓库当前提交点为 `376856f checkpoint Caddie agent workspace redesign`。
- 【事实】本轮直接修复发生在 2026-07-27 前后，触发事件是用户在“国泰海通股权业务实习生”工作台中连续指出三份资料明明存在，Agent 却反复要求重新提供。
- 【未知】更早版本中每一行 Agent Runtime、MCP 和三栏工作台代码分别由哪个 Agent 编写，我没有足够证据逐行归属，因此不据为己有。

### 我直接修改、查看或验证过的对象

**代码与测试**

- `context.py`
- `retrieval.py`
- `server.py`
- `db.py`
- `tests/test_track_knowledge_recall.py`
- `tests/test_external_agent_protocol.py`

**数据库对象**

- `knowledge_folders`
- `knowledge_items`
- `feedback_notes`
- `agent_tasks`
- `agent_runs`
- `proposed_changes`（通过接口和测试查看）
- `workspace_change_events`
- 岗位 `job_track #20` 及其“面试准备”知识目录（运行时只读核验）

**页面与功能**

- Caddie 工作区三栏 Agent 页面
- 岗位档案中的 Agent 工作台
- 右侧“候选产出 / 写入结果”区域
- 本地文件夹资料引用
- “整理入库 / 写入档案 / 形成行动”
- 对话编辑、撤回/截断、重新生成和会话恢复

### 知道、判断、推测的区分

- 【事实】我亲自知道：上述代码当前如何检索、如何拼上下文、如何注入反馈、如何约束写入；国泰海通三份文档确实存在；相应回归测试通过。
- 【判断】这些基础设施的产品意义，是把一次对话中的建议变成可追溯、可确认、可继续使用的职业资料更新。
- 【假设】长期使用后，反馈约束会让结果“越用越像用户”。代码支持这一方向，但我没有留存率、复用率或长期质量对照数据。
- 【未知】Caddie 是否已经找到足够广泛的目标用户、是否优于用户现有工作流、是否有除开发者本人以外的稳定真实用户。

---

## 1. 我接到的原始任务

### 1.1 用户最初要求解决什么

【事实】在我实际参与的阶段，用户并不是要求“再加一个聊天框”，而是要求 Agent 能完成以下连续任务：

1. 读取已经存放在岗位工作台、知识库或本地目录中的资料；
2. 根据用户当前目标自动找到需要的内容，而不是再次让用户上传；
3. 对长资料进行详细整理，不能只输出高度摘要；
4. 判断同一项目的多份资料应合并还是新增，不能把一个项目拆成多个假项目；
5. 在资料不完整或存在冲突时用可交互问题向用户确认；
6. 把确认后的内容写入正确的岗位、项目或知识文档；
7. 保留用户控制权，不得编造事实或用旧候选覆盖用户新修改。

本轮最明确的故障是：用户指出“国泰海通股权知识里面有西安旅游、IPO 论文、讯飞医疗 IPO”，Agent 多次回答“没有找到”“请重新提供”。实际数据库中三份文档均存在。

### 1.2 用户提供的背景、案例和材料

【事实】用户提供或在产品中已经存有：

- 国泰海通股权业务实习生岗位档案；
- “面试准备”文件夹；
- `knowledge_item #55`：`2.讯飞医疗IPO案例分析`；
- `knowledge_item #57`：`4.IPO毕业论文设计`；
- `knowledge_item #58`：`5.西安旅游公司案例`；
- 用户连续对话中的指代：“这三个项目”“放进我的经历中的项目中”；
- 大量本地实习材料目录，例如滴滴、美团、华兴工作文件；
- 用户对错误写入、资料丢失、内容过度压缩和交互低效的截图反馈。

### 1.3 我最开始如何理解需求

【判断】我把问题拆为四层，而不是只改提示词：

1. **存在性层**：系统必须先知道岗位里有哪些真实文档；
2. **召回层**：用户点名或模糊指代时，必须能找到正文；
3. **语境层**：当前句“把这三个放进去”必须继承前几轮提到的文档名称；
4. **执行层**：找到资料后，写入必须经过类型校验、对象绑定、用户确认和版本冲突检查。

### 1.4 最初提出或实现的方案

【事实】最终落地的第一组方案包括：

- 在每轮岗位上下文中注入最多 120 项的完整知识标题目录；
- 明确区分“目录中存在”与“本轮正文被检索到”；
- 在向量/关键词混合检索之外增加确定性的标题匹配；
- 把最近 4 轮用户消息与当前消息合并成检索查询；
- 对 `i o p`、`IOP` 做窄范围归一化；
- 加入系统规则：正文未加载不得推断为文档不存在；
- 新增回归测试，模拟国泰海通三份资料同时被点名的真实故障；
- 沿用并验证候选修改、确认写入和旧版本冲突保护。

---

## 2. 用户如何纠正我

### 纠正一：从“上传资料”改为“按需读取本地资料”

- **我的原方案：** 让用户通过收件箱上传文件，或在输入框上方逐个挂载资料卡片。
- **用户为什么不满意：** 上传和逐项勾选把资料查找责任重新交给用户；大量文件卡片挤占输入区，视觉混乱，也不符合“像 Codex 一样按任务读取”的预期。
- **用户实际想要的：** 只给 Agent 一个本地目录或对象范围，Agent 根据问题自动检索需要的文件；用户不需要逐个挑文件。
- **最终如何修改：** 本地目录作为一个紧凑的范围对象显示，后端按目录建立候选和检索；上下文按任务自动选择正文，而不是把全部文件平铺进输入框。
- **这次修改反映了什么产品判断：** 用户愿意指定边界，但不愿承担检索策略。边界选择是人的权力，边界内的信息发现应由 Agent 完成。
- **证据位置：** 用户截图反馈“首先不要这样显示……不需要勾选……不能像 Codex 一样按我的需要进行读取吗”；`context.py` 的任务范围装配和 `retrieval.py` 的混合检索。

### 纠正二：从“生成摘要”改为“保留证据细节的文档整理”

- **我的原方案：** 把多份资料高度总结为几条经历或多个项目摘要。
- **用户为什么不满意：** 细节、动作、迭代和贡献边界丢失；同一项目被拆成多个项目；摘要不能继续用于面试深挖或简历证据核查。
- **用户实际想要的：** 同一项目的多份资料先归并，形成详细 Markdown；长内容在右侧独立浏览；摘要只是入口，不是最终资产。
- **最终如何修改：** 候选内容支持长 Markdown 产物和右侧完整查看；写入前区分 `merge_to_existing` 与新增项目；资料正文和出处保留在 provenance 中。
- **这次修改反映了什么产品判断：** 求职资料整理的目标不是“读完有一个摘要”，而是形成后续简历、面试、复盘可反复取证的母本。
- **证据位置：** 用户原话“返回的东西太高度总结了，细节都没有了”“明显是同一个项目的，他却生成了几个项目”；`assets.provenance_json`、候选修改协议及长文档预览页面。

### 纠正三：从“文字追问”改为“可直接回答的问题卡”

- **我的原方案：** Agent 在右栏列出多段“待确认”文字，让用户自己回到输入框回答。
- **用户为什么不满意：** 问题不能直接交互；多个问题混在说明文字里；用户需要自己组织答案并来回对照。
- **用户实际想要的：** 单选、多选、自由补充等结构化问题卡，一次回答一组必要问题，提交后 Agent 自动继续。
- **最终如何修改：** 澄清协议升级为结构化问题对象；前端按问题类型渲染选项，允许批量提交和必要的自由补充；旧模型返回纯文本时保留兼容输入。
- **这次修改反映了什么产品判断：** AI 的不确定性不能只通过“我不确定”展示，还要转化为最低摩擦、可完成的用户决策。
- **证据位置：** 用户原话“以问题选项的形式问我，而不是这样不能交互的问我”；统一 Agent 工作台右侧确认区的交互迭代记录。

### 纠正四：从“AI说已写入”改为“真实可验证的候选写入”

- **我的原方案：** Agent 在聊天中说明“已整理”“点整理入库即可”，但执行对象和参数未被严格校验。
- **用户为什么不满意：** 出现 `type 'list' is not supported`、把本地路径当成整数项目 ID、引用不存在的 `主资料 #80` 等错误；聊天说成功不等于数据库成功。
- **用户实际想要的：** AI 必须生成明确的动作、目标对象和正文；系统在写入前做类型与对象校验；结果区展示逐项成功或失败；失败可修复后重试，不能假装完成。
- **最终如何修改：** 候选修改与执行分离；写入动作绑定 `target_type/target_id`；来源和目标 ID 分开；参数序列化；执行结果逐项返回；外部 Agent 只能提案，不能绕过确认。
- **这次修改反映了什么产品判断：** “AI 会调用工具”不是价值，可靠地把自然语言意图映射到正确业务对象才是。工具调用的成功状态必须来自后端事实。
- **证据位置：** 多张“写入失败”截图；`tests/test_external_agent_protocol.py`；`context.py:993-1075`。

### 纠正五：从“自动写入更智能”改为“有证据也要轻确认”

- **我的原方案：** 对能从原文抽取出的事实尽可能自动入库，以减少用户操作。
- **用户为什么不满意：** 即使读到了原句，AI 仍可能误解数字口径、时间范围、个人与团队贡献；错误一旦成为长期事实，会污染后续简历和面试话术。
- **用户实际想要的：** AI 提取候选事实并展示证据，用户可以编辑、确认或拒绝；AI 不得把推断包装成事实。
- **最终如何修改：** 正式事实、硬反馈和覆盖性文档修改都进入确认；用户编辑后的版本优先；无证据提案可拒绝；保留 Agent 原提议用于追溯。
- **这次修改反映了什么产品判断：** 对求职资料而言，“减少一次点击”不如“避免长期错误”重要。高影响信息应把最终判断权留给用户。
- **证据位置：** `docs/agent-protocol/06-PROVISIONAL-UX-DECISIONS.md:47-60,94-102`；`tests/test_external_agent_protocol.py:205-233`。

### 纠正六：从“Top-K没召回就是没有”改为“存在性与相关性分离”

- **我的原方案：** Agent 主要依赖当前一句话做 Top-K 检索；没进入本轮正文的文档不会出现在上下文里。
- **用户为什么不满意：** 三份文档实际存在，但 Agent 连续回答“没有找到”，还要求用户再次定位或粘贴。用户已经明确说资料在“国泰海通股权业务实习生”的准备知识中。
- **用户实际想要的：** 系统先承认目录中的真实存在，再解决正文读取；用户连续指代必须继承；一次点名多份资料不能只找到一份。
- **最终如何修改：** 完整目录常驻上下文；标题精确匹配补充混合检索；最近四轮用户消息进入查询；加入缩写归一化；系统提示禁止把未召回解释为不存在。
- **这次修改反映了什么产品判断：** 检索分数只能回答“本轮最相关什么”，不能回答“数据库里有没有”。AI 产品必须把检索不确定性显式建模，不能让模型用流畅语言掩盖信息缺失。
- **证据位置：** `context.py:555-585,629-650`；`retrieval.py:71-85`；`server.py:12460-12467,12569-12582`；`tests/test_track_knowledge_recall.py`。

### 纠正七：从“Agent结果优先”改为“用户新修改绝对优先”

- **我的原方案：** Agent 根据读取时的文档生成候选，之后直接应用。
- **用户为什么不满意：** 候选生成后用户可能已修改原文；旧候选覆盖新内容会让用户失去控制，也会产生难以发现的数据回退。
- **用户实际想要的：** 写入前重新读取目标并比较版本；发生变化时拒绝覆盖，要求重新生成。
- **最终如何修改：** 上下文包携带变更游标和对象指纹；候选保存 `base_hash`；应用前检查冲突；用户编辑后的最终版本进入下一轮上下文。
- **这次修改反映了什么产品判断：** 用户不是 Agent 流程里的审核员，而是事实和正式表达的所有者；Agent 的计算结果具有时效性。
- **证据位置：** `context.py:993-1002,1060-1075`；`tests/test_external_agent_protocol.py:320-345`。

---

## 3. 模块解决的真实问题

### 3.1 服务的具体用户任务

【判断】该模块服务的不是“和 AI 聊职业问题”，而是：

> 用户已经积累了大量简历、项目文档、面试材料和纠正意见，希望在一个具体求职目标下，让 Agent 找到正确证据、形成可复用内容，并在自己确认后更新到正确档案。

### 3.2 没有模块时如何完成

【事实】用户原来会：

1. 在 Finder、下载目录、聊天记录和多个 Word/Markdown 中找材料；
2. 手动复制给 ChatGPT、Claude 或 Codex；
3. 反复解释“这是同一个项目”“这个数字不是我独立完成”“这份材料在另一个对话里”；
4. 把对话结果再复制回 Word、Markdown 或求职看板；
5. 自己判断新版本是否覆盖了旧细节。

### 3.3 原方法最困难的地方

- 【判断】不是单次生成慢，而是资料定位、上下文复原和结果回填都依赖用户记忆。
- 【事实】同一岗位中已经存在的文档仍可能因检索窗口、标题变体或对话指代而漏掉。
- 【判断】AI 生成的流畅文本很难暴露它究竟读了什么、漏了什么、把哪句话当成事实。
- 【判断】对话结果与正式档案之间没有天然事务边界，容易出现“聊天里说写了，库里没写”“旧结果覆盖新修改”。

### 3.4 改变的是效率、质量、认知、决策还是行为

- **效率：** 自动定位岗位范围内的资料，减少重复上传和跨文件复制。
- **质量：** 强制区分目录存在、正文命中、模型推断和用户确认，减少漏文档与编造。
- **认知：** 用户能看到 Agent 使用了哪些资料、哪些地方不确定、哪些内容将成为长期事实。
- **决策：** 对合并/新增、贡献边界、数字口径和正式写入保留用户判断。
- **行为：** 用户从“自己维护一套复制粘贴流程”转为“给目标和边界、审查关键候选、让系统持续维护对象”。

### 3.5 使用前后差异

**使用前**

- 用户负责找文件、拼上下文、判断重复、复制结果和维护版本；
- AI 只负责当前对话中的理解与生成；
- 用户纠正通常停留在当前会话。

**使用后（当前能力边界内）**

- 用户指定岗位、项目或本地目录范围；
- 系统提供完整目录并按目标检索正文；
- Agent 生成候选修改并标注来源；
- 用户编辑、确认或拒绝；
- 系统写入正确对象并记录变更；
- 生效反馈在后续上下文中重新注入。

### 3.6 真实使用案例

**案例：把国泰海通三份项目材料纳入经历档案**

- 【事实·输入】用户在“国泰海通股权业务实习生”工作台中说，准备知识里有“西安旅游”“讯飞医疗 IPO”“IPO 论文”，要求放进个人经历项目。
- 【事实·原故障】Agent 只看到部分 Top-K 结果，先说只有复盘报告，后又说找不到独立文档，并要求用户重新粘贴。
- 【事实·核验】运行时数据库中存在：
  - `knowledge_item #55` 讯飞医疗 IPO 案例；
  - `knowledge_item #57` IPO 毕业论文设计；
  - `knowledge_item #58` 西安旅游公司案例。
- 【事实·修复操作】加入完整知识目录、标题确定性匹配、多轮用户意图、`i o p → IPO` 归一化和禁止假阴性的系统规则。
- 【事实·验证输出】回归测试在关闭向量检索的情况下仍能同时加载三份正文；真实检索接口也返回三份目标资料。
- 【未知·后续用途】“三份内容最终是否已经成功写入个人经历项目”未在本轮完成后再次由用户验收，因此不能声称最终归档闭环已经完成。

---

## 4. AI 在其中的作用

### 必须由 AI 完成

#### A. 从自然语言目标判断读取范围和意图

- 【判断】AI 需要理解“把这三个放进去”中的“三个”指什么、目标是新增项目还是合并既有经历、用户说的“股权知识”对应哪个岗位范围。
- 固定规则可以保留最近消息，却无法独立完成所有指代消解和业务意图判断。
- 输出会随岗位、历史对话、资料目录和用户反馈变化。
- 错误后果是读错岗位、漏资料、把同一项目拆成多个项目，或写进错误档案。

#### B. 跨文档归并和贡献边界判断

- 【判断】AI 需要识别多份文件是同一项目的阶段材料、不同项目，还是一份总述与若干证据。
- 仅按标题、路径或哈希无法判断“ML Planner 周报 v0.6-v0.8”是版本链，还是三个独立项目。
- 输出必须根据正文中的背景、动作、角色、数字和时间关系变化。
- 错误后果是经历结构失真、重复项目、数字与个人贡献被错误放大。

#### C. 把不完整信息转化为最少必要澄清

- 【判断】AI 需要判断哪些缺口会阻止安全写入，例如项目归属、个人角色、数字口径；哪些可以先形成草稿。
- 固定表单无法预先覆盖每类项目的未知信息，也会给用户制造大量无关问题。
- 错误判断可能导致不必要的追问，或把高风险未知内容写成事实。

#### D. 根据反馈约束调整后续生成

- 【事实】`feedback_notes` 会按 global、track、project 范围进入上下文。
- 【判断】AI 需要理解“不要把团队测试结论写成我独立完成”如何影响不同段落，而不是机械删除某个词。
- 错误后果是同一类错误在简历、面试话术和项目总结中反复出现。

### AI 可以增强，但并非必要

1. **查询扩展和错别字修复**：可以用固定词典完成常见缩写；AI 可处理更复杂的语义变体。
2. **标题、摘要和标签生成**：人工可填写；AI 降低整理成本。
3. **长 Markdown 的结构优化**：用户或模板可完成；AI 可根据材料决定哪里用叙述、哪里用要点。
4. **重复项目建议**：哈希、标题和时间规则可先筛；AI 负责解释语义上的合并理由。
5. **问题卡选项生成**：固定表单可以问，但 AI 能根据当前冲突给出更贴近材料的选项。

### 普通软件能力

- SQLite 存储和 CRUD；
- 文件夹、知识条目和岗位关联；
- 本地文件读取、路径授权和索引；
- 版本号、哈希、变更游标和冲突检测；
- 候选修改状态、确认按钮、失败重试；
- Markdown 预览、下载、折叠、筛选；
- 聊天记录保存、重命名、分类、截断旧分支；
- Git 镜像、diff 和回滚；
- 权限范围和对象 ID 校验。

【判断】这些能力决定系统是否可靠，但不应包装成“AI 会思考”的亮点。

---

## 5. 人与 AI 如何共同工作

1. **AI 提出什么？**  
   【事实】AI 提出相关资料集合、合并/新增判断、结构化草稿、待确认事实、澄清问题和候选写入动作。

2. **用户在哪一步判断、修改或确认？**  
   【事实】用户在资料范围、事实口径、个人贡献、正式文档覆盖、长期反馈和外部动作上确认；可直接修改候选内容后再应用。

3. **一次纠正是否影响后续结果？**  
   【事实】可以。确认保存为 `feedback_notes` 的纠正会按范围重新注入上下文；用户修改后的正式对象成为下一轮读取版本。

4. **影响如何发生？**  
   【事实】`context.py` 读取 global/track/project 范围的 active feedback，区分 hard/soft 约束；`workspace_change_events` 和对象哈希让后续 Agent 读取最终确认版本，而不是旧候选。

5. **用户不参与判断，产品是否仍成立？**  
   【判断】只做检索、讲解和临时草稿时可以成立；一旦要把数字、贡献边界、正式经历或长期反馈写入，用户完全不参与就不成立。求职资料的事实所有权不能交给模型。

6. **体现怎样的人机分工？**  
   【判断】机器负责扩大阅读范围、发现关系、生成候选和执行重复操作；人负责目标、事实归属、风险取舍和最终表达。这里的人不是“帮 AI 擦屁股”，而是保留对职业身份的编辑权。

### 最能体现 AI 不确定性与人类控制权的设计

【判断】不是“所有写入都弹确认框”，而是以下组合：

> 完整目录证明对象存在 → 检索正文并展示来源 → AI 形成候选 → 对数字、贡献和覆盖性修改轻确认 → 用户编辑优先 → 写入前再检查版本冲突。

它同时承认三类不确定性：AI 可能没召回、可能理解错、候选生成后现实状态可能已变化。

---

## 6. 与 Skill 或通用对话的关系

### 6.1 可以直接写成 Skill 的能力

- 将一份项目文档整理为 STAR；
- 从 JD 提取要求；
- 把面试逐字稿转成复盘；
- 生成简历 bullet；
- 按固定格式提取事实和追问；
- 对单批文件做去重、摘要和分类；
- 生成澄清问题或候选标签。

### 6.2 ChatGPT 或 Claude 对话即可完成的能力

- 一次性的岗位分析；
- 针对一份已上传简历给修改建议；
- 讨论某项目应该怎么讲；
- 生成一版自我介绍、邮件或面试答案；
- 在用户完整提供上下文的前提下比较几份材料。

### 6.3 只保留 Skill 会退化的任务

1. **跨次任务定位已有资料**：Skill 不天然知道三份资料挂在哪个岗位、哪个文件夹、哪个版本。
2. **正式对象更新**：Skill 可以输出 Markdown，但不会自动知道应更新项目 #18、知识 #55 还是岗位 #20。
3. **用户纠正的范围传播**：一次“贡献边界不对”需要决定只影响某项目还是全部简历。
4. **并发版本保护**：Skill 生成期间用户可能修改原文，需要对象哈希和冲突检测。
5. **长期可审计性**：需要保留原提议、用户编辑、证据、确认和最终版本之间的关系。

### 6.4 退化发生在哪一步

【判断】不是生成步骤退化，而是“读取正确上下文 → 绑定正确对象 → 用户确认 → 持久写入 → 下次继续使用”的连续链路断裂。用户必须重新充当检索器、数据库映射器和版本管理员。

### 6.5 独立产品形态是否必要

【判断】对一次性求职问答，不必要；一个 Skill 或通用对话更轻。  
【判断】对资料多、岗位多、持续修改、需要把反馈作用到后续产物的用户，独立对象层和确认层有必要。但这只证明“需要一个持久工作区”，尚不能证明当前 Caddie 的全部页面和信息架构就是最佳形态。

### 6.6 不能只用“长期记忆”解释的必要性

独立产品真正承担的是：

- 同一事实在不同岗位表达中的“引用而非复制”；
- 正式对象与聊天草稿的边界；
- 数字、贡献和版本的证据链；
- 对写入动作的对象级权限与冲突控制；
- 用户能看到并改写 Agent 将要改变的内容。

这些是业务对象和事务问题，不只是模型记忆问题。

---

## 7. 模块在整个产品中的位置

### 定位选择

- **知识检索与上下文装配：核心闭环的一部分。**
- **候选修改、确认、版本冲突保护：支撑基础设施。**
- **多专家视觉人设与过程展示：后来扩展的能力，部分仍属已实现但尚未验证。**

### 7.1 删除后核心用户价值是否成立

- 【判断】删除混合检索和上下文装配后，用户仍可手动上传、复制和让 AI 生成内容，因此求职辅导功能还能手动完成。
- 【判断】但“无需反复喂料、能使用已有资料、纠正能影响后续”的核心闭环不再成立。
- 【判断】删除安全写入后，用户仍能手工复制结果回文档；产品不会完全失效，但会退化为聊天工具加资料展示页。

### 7.2 上游依赖

- 岗位、项目、经历等对象模型；
- `knowledge_folders` 和 `knowledge_items`；
- 原始资料或本地文件读取；
- 聊天历史；
- `feedback_notes`；
- 模型供应商与能力路由。

### 7.3 下游使用

- 岗位对话和岗位研究；
- 项目经历整理；
- 简历、面试话术和知识文档候选；
- 外部 Agent 上下文包；
- 后续的模拟面试和复盘（是否全部已接通，需按具体入口核验）。

### 7.4 是否把真实反馈传回上游

- 【事实】用户确认保存的反馈会进入 `feedback_notes`，后续上下文会按作用域读取。
- 【事实】用户修改后的对象通过版本和变更事件成为下一轮 Agent 的读取对象。
- 【未知】这些反馈是否已经在长期真实使用中显著改善生成质量，没有对照实验或持续使用数据。

---

## 8. 证据与成熟度

### 8.1 相关文件路径

- `context.py`：上下文装配、完整知识目录、标题匹配、反馈注入、外部 Agent 上下文包。
- `retrieval.py`：查询归一化、FTS 与本地向量混合检索。
- `server.py`：岗位对话系统规则、多轮用户意图拼接、检索和消息保存。
- `db.py`：知识、反馈、Agent Runtime、职业事实和变更事件数据表。
- `tests/test_track_knowledge_recall.py`：国泰海通三文档召回回归测试。
- `tests/test_external_agent_protocol.py`：确认写入、拒绝无证据事实、版本冲突测试。
- `docs/Caddie-开发过程实录.md`：propose-confirm、上下文漂移和真实迭代记录。
- `docs/agent-protocol/06-PROVISIONAL-UX-DECISIONS.md`：有证据事实仍需确认、用户编辑优先、最小读取范围。
- `docs/MEMORY-产品决策.md`：当前 Agent Runtime 与不可违背原则的仓库记录。

### 8.2 关键代码与数据表

- 【事实】`context.py:555-580`：完整岗位知识目录，并明确“正文未进入上下文不等于不存在”。
- 【事实】`context.py:629-650`：混合检索外的标题确定性匹配。
- 【事实】`context.py:993-1075`：最小权限上下文包、变更游标和写入规则。
- 【事实】`retrieval.py:71-85`：用户查询变体归一化。
- 【事实】`server.py:12460-12467`：禁止把未召回解释为不存在。
- 【事实】`server.py:12569-12582`：最近四轮用户消息参与检索。
- 【事实】`db.py:428-509`：反馈、资产、简历版本、知识目录和知识条目。
- 【事实】`db.py:516-558,624-638`：Agent 任务、运行和工作区变更事件。

### 8.3 对应页面

- 全局“问 Caddie / Caddie 工作区”；
- 岗位档案右上角“Agent 工作台”；
- 三栏工作台：任务与过程 / 正文与对话 / 候选修改；
- 知识空间文档工作区；
- 岗位“准备知识”；
- 右侧“写入失败 / 已写入 / 待确认”状态区。

### 8.4 Git 提交记录

- `376856f checkpoint Caddie agent workspace redesign`
- `dbfe1fa 完善外部 Agent 协作与 Caddie Alpha 基础设施`
- `9e8abd8 重构求职工作台并新增岗位简历版本管理`
- `25a097e 求职目标工作台：投递关联 + 两栏布局 + 目标对话台`
- `e3f89c1 新增作品集、求职目标工作台、面试关注点自迭代；跨平台支持`

【说明】这些提交证明相关阶段和代码存在，不证明全部内容由本证词 Agent 独立完成。

### 8.5 用户原话或需求记录

- “是这样的，最好他能直接给他一个本地的项目，就像 Codex 一样，不用上传收件箱，直接从本地读取然后写入。”
- “首先不要这样显示，显得很乱，其次不需要勾选啊……为什么还要我勾选。”
- “以问题选项的形式问我，而不是这样不能交互的问我。”
- “返回的东西太高度总结了，细节都没有了。”
- “明显是同一个项目的，他却生成了几个项目，这个不行。”
- “不是复盘报告，是准备知识中的面试准备中有相关文档信息。”
- “时间不重要，你放进去就行，内容在文档里面写的有。”
- “有文档，但是找不到？我不明白你的检索怎么做的。”

### 8.6 真实数据

- 【事实】运行时岗位 `job_track #20`：国泰海通股权业务实习生。
- 【事实】知识条目 #55、#57、#58 分别对应讯飞医疗 IPO、IPO 论文和西安旅游案例。
- 【事实】回归测试可在向量检索返回空数组时，仍通过标题与目录同时加载三份正文。
- 【事实】外部 Agent 协议测试覆盖 7 场景事实的用户确认措辞、无证据事实拒绝和旧版本冲突。

### 8.7 已完成但没有使用证据的能力

- 【事实】Agent Runtime、专家档案、外部 Agent 上下文包、候选修改和冲突保护已有代码与测试。
- 【未知】普通用户是否理解专家分工，是否会主动使用岗位研究员、经历侦探等入口。
- 【未知】结构化问题卡是否显著降低澄清成本，没有行为数据。
- 【未知】完整目录常驻上下文在超大知识库中的成本与效果上限。

### 8.8 仍停留在设想中的能力

- 【假设】反馈自动识别受影响资产并批量标记过期；
- 【假设】跨平台社交内容检索和视频理解；
- 【假设】完整多专家自主编排；
- 【假设】用真实长期数据证明“越用越懂用户”；
- 【假设】无需用户选择范围、同时又能稳定保护隐私的全自动本地文件读取。

### 8.9 成熟度结论

- 【事实】知识目录、混合检索、多轮意图、反馈注入、候选写入和冲突保护有代码与测试。
- 【判断】工程上已形成可运行骨架，产品上仍处于高频人工验收驱动的 Alpha 阶段。
- 【未知】模块对最终求职结果、面试率或资料维护成本的量化改善。

### 工程完整性与 AI 产品判断的区分

**工程完整性**

- 数据表、CRUD、索引、序列化；
- 文件读取和路径处理；
- 失败状态、重试、日志；
- 会话保存和 UI 恢复；
- 哈希、版本、游标、事务与回滚；
- 自动化测试。

**AI 产品判断**

- 目录存在与正文召回必须分开；
- 用户只选择边界，不负责边界内逐文件检索；
- 同一项目优先合并证据，不按文件数量制造项目；
- 有证据的事实仍需对数字和贡献做轻确认；
- 用户编辑优先于 Agent 候选；
- 不确定性应变成可回答的问题，而不是一句免责声明；
- 对话输出不等于正式档案，写入必须成为显式、可审查的动作。

### 为什么这些能力不应当作为视频开场

【判断】检索、确认、版本冲突和写入协议主要解决的是“Agent 如何安全操作产品”，不是用户第一次使用 Caddie 时最直观的目标。它们缺少独立的情绪张力，也需要先解释数据库和模型不确定性。

视频开场如果先讲“混合检索、哈希冲突、候选修改”，会把 Caddie讲成 Agent 基础设施。更合理的证据顺序是：先展示用户把散落的真实资料变成某个岗位可执行准备的完整流程，再在出现“资料已存在但 AI 没读到”“事实不能擅自写”时解释这些设计为何必要。

---

## 9. 最重要的结论

### 1. 最有价值的产品判断是什么？

【判断】“检索没有命中”绝不等于“资料不存在”。Caddie 必须先维护可验证的对象目录，再让 AI 做相关性选择；模型不能用一次 Top-K 结果替代数据库事实。

### 2. 用户做了哪一个我作为 Agent 不会自然做出的决定？

【判断】用户拒绝了“自动写入才显得智能”，坚持即使原文有证据，数字口径、时间范围和个人贡献仍要由人确认。Agent 自然追求减少步骤，用户选择了保留职业事实的所有权。

### 3. 哪个能力看起来很炫，但不应成为主要亮点？

【判断】多专家人设、模型自动路由和可视化执行过程。它们可以改善复杂任务，但没有证据证明用户因为“有七个专家”而获得更好的求职结果；核心仍是资料是否读对、判断是否可信、修改是否真正进入后续流程。

### 4. 哪个真实流程最值得在三分钟 Case Study 中展示？

【判断】展示“国泰海通三份资料归档”故障及修复：

1. 用户指出岗位准备知识里已有三份项目材料；
2. Agent 因 Top-K 和单轮查询漏掉文档，错误要求重新上传；
3. 产品把完整目录、正文召回、多轮指代和确认写入分成四层；
4. 修复后同时读取三份资料，判断合并/新增并形成候选；
5. 用户确认后才写入，旧版本不得覆盖新修改。

这条流程同时包含真实用户痛点、AI 不确定性、检索设计、人类控制和可验证结果。

### 5. 关于 Caddie，我最没有资格下结论的部分是什么？

【未知】我没有资格判断 Caddie 是否已经成为一个有市场需求、能让广泛用户长期迁移工作流、并能提升实际求职成功率的完整产品。我能证明的是这一套 Agent 读取与写入基础设施为何出现、修复了哪些真实错误，以及它目前能做到和仍不能证明的边界。

