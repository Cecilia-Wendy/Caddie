# Caddie 模块构建证词：统一 Agent 工作台与面试反馈入口（Codex 重设计阶段）

> 证词范围：仅覆盖我在 2026-07-27 这一轮对话中亲自参与、直接查看或能由仓库文件和当前对话证明的内容。  
> 证词人：Codex（本轮参与 Caddie 工作台重设计的 Agent）  
> 对应提交：`376856f checkpoint Caddie agent workspace redesign`

## 证词边界

### 我负责或深度参与过的模块

- 【事实】我深度参与了 Caddie 全局对话工作区的 Codex 式重设计，包括任务栏、主对话区、Agent 执行过程、右侧文档与产出区、专家选择、左右栏收放和宽度调整。
- 【事实】我参与了项目专家、岗位 Agent、知识专家、简历诊断等入口的盘点，并检查了它们是否进入同一套 Agent Runtime 或不同的旧工作台。
- 【事实】我直接参与了“文档修改必须展示目标路径并提供直达按钮”的交互设计与实现核对。
- 【事实】我直接检查了面试复盘、面试题深度批改、多轮教练对话、模拟训练以及结果写入文档的代码和页面。
- 【事实】我提交了当时工作树中与 Agent 工作台、面试系统和相关后端配套有关的 8 个已跟踪文件，形成提交 `376856f`。
- 【判断】我参与的核心不是重新定义 Caddie 全部产品，而是把已经存在的 Agent、专家和文档能力整理成更清晰的工作界面，并识别重复入口。

### 大致参与阶段

- 【事实】参与日期为 2026-07-27。
- 【事实】此时仓库已经存在岗位、项目、简历、知识、面试、邮箱、外部 Agent 接入等模块。我不是这些模块从零到一的唯一构建者。
- 【判断】我参与的是“已有能力较多，但 Agent 入口、对话框和信息结构不统一”的重设计与收敛阶段。

### 我直接修改或查看过的证据

我直接修改、检查或提交过：

- `static/index.html`
- `static/caddie-ui.css`
- `server.py`
- `db.py`
- `interview_store.py`
- `static/interview-system.css`
- `static/interview-system.js`
- `tests/test_interview_question_assets.py`
- `agent_runtime.py`
- `tests/test_agent_project_update.py`
- `tests/test_agent_global_knowledge.py`

我直接查看过的页面或功能：

- Caddie 全局对话工作区
- 项目详情与“项目专家团”
- 岗位档案与岗位 Agent 工作台
- 简历版本列表与“AI 诊断”
- 知识文档与“知识专家”
- 首页“今天”
- 面试与复盘页面
- 模拟训练
- Agent 更新审核
- 邮箱助理入口

我直接查看过的数据对象：

- `agent_experts`
- `agent_tasks`
- `agent_runs`
- `agent_events`
- `proposed_changes`
- `knowledge_agent_runs`
- `knowledge_agent_changes`
- `interview_rounds`
- `interview_question_coaching_versions`
- `interview_question_coaching_messages`
- `career_calendar_items`

### 亲自知道、推测与未知

- 【事实】我亲自知道当前代码存在 7 个专家定义、统一 Agent 任务/运行/事件/候选修改结构，以及多套前端 Agent 入口。
- 【事实】我亲自知道用户连续要求界面参考 Codex，而不是继续保留沉重、重复的独立对话框。
- 【事实】我亲自知道用户要求文档产出必须显示路径和直达按钮，并坚持面试复盘需要多轮对话调整。
- 【判断】我认为“岗位、项目、简历、知识和面试”应该共享同一套工作台外壳，但这是本轮形成的产品判断，不代表整个产品已经完成统一。
- 【假设】统一工作台可以降低用户对多个专家入口的理解成本；当前没有可引用的正式可用性实验。
- 【未知】我不知道全部历史用户如何使用 Caddie，也不知道所有模块的真实留存和频率。
- 【未知】我没有成功读取本机正式数据库的实时对象数量，因此不会把表结构存在写成真实使用规模。

---

## 1. 我接到的原始任务

### 1.1 用户最初要求解决什么问题

- 【事实】用户最初认为 Caddie 对话框“太丑”“很笨重”“看着很劣质”，要求完全复刻 Codex 工作台，或者参考 WorkBuddy。
- 【事实】用户提供的目标不是换颜色或增加装饰，而是改变整个对话工作区的布局、信息层级和交互逻辑。
- 【事实】后续任务逐渐扩展为：
  - 对齐 Codex 的左侧任务栏、主对话区和输入框；
  - 右侧展示文档、产出和 Agent 执行结果；
  - 左栏和右栏可收放；
  - 中间区域随两侧栏自由伸缩；
  - 专家团能够被看见和手动选择；
  - 文档修改必须显示路径和直达按钮；
  - 其他 Agent 入口也要统一。

### 1.2 用户提供的背景、案例和材料

- 【事实】用户连续提供了多张 Caddie、Codex 和 WorkBuddy 截图，并用红框标注：
  - Caddie 原对话框的笨重区域；
  - Codex 左栏收放按钮；
  - Codex 右侧栏开关；
  - Codex 顶部紧凑任务栏；
  - Codex Agent 执行轨迹；
  - WorkBuddy 专家团入口和专家库；
  - Caddie 中过宽的分隔带、重复关闭按钮、过大的图标和空白区。
- 【事实】用户给出的真实对话案例包括：
  - 收件箱新增项目和表述，需要整理进入经历；
  - 判断一段经历是否值得写入简历；
  - 修改简历但找不到写入位置；
  - 查看 Agent 执行过程；
  - 进入知识文档、岗位资料和面试复盘。

### 1.3 我最开始如何理解需求

- 【判断】我最初把它理解为一个“视觉重设计 + 工作区三栏化”任务，即左侧历史任务、中间对话、右侧上下文/产出。
- 【判断】我一开始对 Codex 的理解偏向表面布局：收窄边栏、调整顶部、增加右侧栏、压缩输入框，而没有充分把握用户要求的是 Codex 的定位逻辑、执行过程和文档产出闭环。

### 1.4 我最初提出或实现的方案

- 【事实】我首先把 Caddie 对话区改造成类似 Codex 的工作台结构：
  - 左侧任务列表；
  - 中间对话与底部输入框；
  - 右侧“文档与产出”；
  - 顶部收放按钮。
- 【事实】后续增加了：
  - 专家团选择；
  - 自动分诊与手动专家；
  - Agent 执行步骤；
  - 文档结果卡；
  - 文档路径和打开按钮；
  - 左右栏拖动调整；
  - 文件夹折叠；
  - 更紧凑的图标与顶部栏。
- 【事实】前端主实现集中在 `static/index.html` 和 `static/caddie-ui.css`；后端专家选择、文档结果和写入动作涉及 `server.py`、`db.py` 与 `agent_runtime.py`。

---

## 2. 用户如何纠正我

### 纠正一：不能只做一个“像聊天网页”的三栏布局

- 你的原方案：把原 Caddie 对话页面改成左侧任务、中间对话、右侧信息栏。
- 用户为什么不满意：用户认为它和 Codex 仍然不一样，元素位置、顶部结构和输入框逻辑没有真正对齐。
- 用户实际想要的：参考 Codex 的工作台逻辑，不只是参考色彩和三栏外形。
- 最终如何修改：进一步压缩顶部栏、让输入区悬浮并跟随主对话区、增加可收放侧栏、执行轨迹和右侧产出区。
- 这次修改反映了什么产品判断：工作台的价值来自任务、执行、产出和上下文之间的关系，不来自“多放一个聊天框”。
- 证据位置：`static/index.html` 中 `renderChatWorkspace` 相关区域；`static/caddie-ui.css` 中 `.chat-workspace`、`.chat-main`、`.chat-aside`、`.composer`。

### 纠正二：右侧不是说明栏，而应该是真正的文档展示区

- 你的原方案：右侧先展示“本轮工作”“使用的上下文”等说明信息。
- 用户为什么不满意：用户明确指出右侧应该像 Codex 一样展示文档和产出，不能只放解释性文字。
- 用户实际想要的：长文档、候选修改、文件路径、执行结果能够在右侧打开，并且不挤压正文到不可读。
- 最终如何修改：右侧工作区增加文档结果卡、文档路径、直接打开能力，并设置宽度上限与可拖动分隔条。
- 这次修改反映了什么产品判断：Agent 不是只负责回答；产出必须成为可查看、可定位、可确认的对象。
- 证据位置：`static/index.html` 中 `renderChatDocuments`、`openChatDocumentResult`；`server.py` 中 `/api/chat/actions/apply` 返回文档信息。

### 纠正三：Agent 执行过程要像 Codex 一样可见

- 你的原方案：以普通助手回复为主，执行过程不够突出。
- 用户为什么不满意：用户给出 Codex 执行状态截图，要求能够看见 Agent 正在读取什么、使用什么工具、处理到哪一步。
- 用户实际想要的：对话区内显示自然的执行轨迹，而不是单独弹出笨重任务窗口。
- 最终如何修改：在对话时间线中加入读取问题、检索资料、组织回答等阶段，并显示处理时长和主理专家。
- 这次修改反映了什么产品判断：用户需要判断 Agent 是否真正读取了相关资料，而不是只看到一段看似完整的文字。
- 证据位置：`static/index.html` 中 `startThinkingProgress`、`execution`、`chat-execution` 相关渲染。

### 纠正四：文档写入不能只说“已保存”

- 你的原方案：回答中出现“写入档案”“保存为知识”等动作按钮，但写入后对路径交代不足。
- 用户为什么不满意：用户明确说：“我需要清晰的知道如果是文档修改，我需要知道修改的文档或者新建文档的路径，需要有一个直达的按钮。”
- 用户实际想要的：区分新建和修改，显示逻辑路径，并能直接打开目标文档。
- 最终如何修改：结果卡展示文档标题、路径、动作类型，并提供“打开文档”等直达按钮。
- 这次修改反映了什么产品判断：AI 写入必须可追溯；否则“保存”只是不可验证的承诺。
- 证据位置：`static/index.html` 的文档结果渲染；`server.py` 的聊天动作应用结果；`db.py` 的候选修改和文档对象。

### 纠正五：专家团不能消失，也不能只是装饰

- 你的原方案：先把右栏重点放在文档和上下文，专家团入口不够清晰。
- 用户为什么不满意：用户问“专家团又去哪里了”，并要求参考 WorkBuddy 的放置和使用方式。
- 用户实际想要的：既能自动分诊，也能手动指定主理专家；专家选择应影响真实后端路由，而不是 UI 标签。
- 最终如何修改：在输入区附近提供专家触发器，右栏可打开专家库；手动选择保存到当前任务，`expert_key` 传入后端。
- 这次修改反映了什么产品判断：专家是用户可理解、可干预的分工机制，不是人格化装饰。
- 证据位置：
  - `static/index.html` 中 `toggleChatExperts`、`selectChatExpert`、`renderChatExpertLibrary`；
  - `server.py` 中 `ChatIn.expert_key`；
  - `db.py` 中 `AGENT_EXPERT_SEEDS`；
  - `agent_runtime.py` 中路由规则。

### 纠正六：布局必须允许用户控制

- 你的原方案：侧栏宽度和中间区域主要由固定布局决定。
- 用户为什么不满意：用户明确要求“中间可以自由缩放，以及左边可以回收，看看 Codex 怎么做的”。
- 用户实际想要的：左栏可折叠、右栏可折叠、两侧可拖动，中间自适应。
- 最终如何修改：增加左右收放状态、拖动分隔条、本地状态保存和响应式宽度约束。
- 这次修改反映了什么产品判断：不同任务需要不同屏幕重心。对话、读文档、看专家时的最佳布局并不相同。
- 证据位置：`static/index.html` 中 sidebar/aside resize 函数；`static/caddie-ui.css` 中 `.sessions-collapsed`、`.aside-open` 和 grid 列定义。

### 纠正七：控件过大和重复会让产品显得笨重

- 你的原方案：顶部右栏开关尺寸较大，右栏内部又有一个关闭叉号。
- 用户为什么不满意：用户指出叉号与顶部收放按钮重复，顶部按钮也太大，应参考左侧收放按钮。
- 用户实际想要的：单一入口、小尺寸、同线宽、同焦点样式。
- 最终如何修改：删除右栏内部叉号，把顶部右栏按钮缩为 `24 × 24px`，图标缩为 `13px`。
- 这次修改反映了什么产品判断：工作台中的结构控制应该安静、稳定，不应和内容操作争夺注意力。
- 证据位置：`static/index.html` 的 `.chat-aside-head`；`static/caddie-ui.css` 的 `.chat-context-toggle`；本轮浏览器验证结果为 `24 × 24`、无 `.chat-aside-close`。

### 纠正八：不能把需要迭代的 AI 功能都降级为一次性任务

- 你的原方案：我曾建议邮箱助理、AI 拆解面试复盘、GitHub 项目解析、岗位画像、差距诊断、外部 Agent 更新审核都不要硬套成聊天，而做成明确的任务型页面或按钮。
- 用户为什么不满意：用户认为面试复盘等场景经常需要多轮调整，不能只得到一次性结果。
- 用户实际想要的：首次执行可以是任务式，但对判断、原话、改写和结论必须能继续多轮对话。
- 最终如何修改：尚未进入下一轮代码重构；产品方案已调整为“任务执行后可无缝进入统一工作台继续讨论”，面试复盘优先保留多轮对话。
- 这次修改反映了什么产品判断：是否使用对话不应按模块名称决定，而应按任务是否需要用户持续纠正判断决定。
- 证据位置：
  - 当前对话用户原话：“特别是面试复盘之类的我觉得还是用对话比较好”；
  - `server.py` 已存在 `/api/v2/interview-questions/{question_id}/coach/messages`；
  - `interview_store.py` 已存在 `interview_question_coaching_messages`。

### 关于“AI不能编造或擅自修改”

- 【事实】本轮用户没有再次完整复述“不能编造”的原则，但用户持续要求看清路径、确认写入和修改对象。
- 【事实】仓库中的专家系统提示词明确要求：
  - 经历侦探区分事实、推断和待补证据；
  - 简历编辑不得假装能修改 WPS 文件；
  - 压力面试官不得替用户回答；
  - 主理人不得声称完成尚未执行的写库操作。
- 【判断】人工确认不是附加安全提示，而是 Agent 产出进入正式职业资料前的产品边界。
- 证据位置：`db.py` 中 `AGENT_EXPERT_SEEDS`；`db.py` 中 `proposed_changes`；`server.py` 中 `/api/agent/tasks/{task_id}/apply`。

---

## 3. 模块解决的真实问题

### 3.1 具体用户任务

- 【事实】用户需要围绕自己的岗位、项目、简历、知识和面试记录进行讨论，并在讨论后形成可继续使用的资料。
- 【判断】真正任务不是“和 AI 聊天”，而是：
  1. 让 AI 读取正确资料；
  2. 判断当前问题应该由谁处理；
  3. 看见处理过程；
  4. 反复纠正；
  5. 将确认结果写入正确对象；
  6. 后续能找到并继续使用。

### 3.2 没有该模块时如何完成

- 【判断】从代码和用户操作案例看，没有统一工作台时，用户需要在项目页、岗位页、知识页、简历页和面试页分别寻找不同的 AI 按钮，再把结果人工复制到文档或资产。
- 【事实】当前代码中确实存在多套入口和旧面板：`openObjectAgent()`、`openSpaceAgent()`、旧 `track-ai-panel`、旧 `knowledgeAgentPanel()`、面试模拟对话和面试题教练对话。

### 3.3 原方法最困难的地方

- 【判断】困难不只是慢，而是对象和边界不清：
  - 不知道 Agent 读了哪些资料；
  - 不知道当前由哪个专家负责；
  - 不知道“保存”写到了哪里；
  - 不知道结果是普通回复、草稿还是正式资料；
  - 不知道从哪个入口继续上一轮任务。

### 3.4 改变的是哪类价值

- 【判断】主要改变四件事：
  - **认知**：知道 Agent 当前的上下文、角色和执行状态；
  - **质量**：通过专家路由、资料检索和用户纠正减少泛化回答；
  - **决策**：用户能比较候选修改并决定是否写入；
  - **行为**：讨论结果不再停留在聊天记录，而进入项目、知识、岗位或面试资料。
- 【判断】效率提升存在，但不是最核心的变化。核心是让一次 AI 输出变成可验证、可纠正、可归档的工作过程。

### 3.5 使用前后差异

使用前：

```text
进入某个页面 → 找到一个 AI 按钮 → 得到一段文字
→ 不知道是否读对资料 → 手动复制 → 之后难以找到
```

使用后（当前设计目标，部分已实现）：

```text
从对象入口进入统一工作台
→ 自动携带岗位/项目/简历/文档
→ 自动分诊或手选专家
→ 看见读取与执行过程
→ 多轮纠正
→ 查看候选文档、路径和差异
→ 确认写入
→ 直接打开正式资料
```

### 3.6 可证明的使用案例

#### 案例：收件箱材料整理为项目经历

- 【事实】用户截图中提出：收件箱新增项目和表述，希望整理进经历。
- 输入：三份材料或项目原文，以及“写得不够详细，再优化一下”的追问。
- 操作：在 Caddie 对话区要求按原文拆解并形成项目经历。
- 输出：候选项目经历、时间、公司/背景、角色、项目描述，以及写入档案等动作。
- 后续用途：进入个人经历、简历或面试准备。
- 【未知】我没有数据库证据证明该截图中的候选最终实际写入了正式项目对象。

#### 案例：面试逐字稿进入复盘和训练

- 【事实】页面允许输入岗位、阶段、复盘目标和原始逐字稿/HR 反馈。
- 操作：调用面试拆解，形成问题、回答、面试官意图、批改和下次答法；用户可保存复盘文档并写入面试总库。
- 输出：复盘文档、结构化问题、长期规则、训练 Prompt、模拟训练上下文。
- 后续用途：现场答法、项目讲法、模拟训练、项目追问、岗位复盘文档。
- 【事实】代码和测试证明这些对象和路径存在。
- 【未知】我没有足够证据说明已有多少真实面试完整走完该闭环。

---

## 4. AI 在其中的作用

### 必须由 AI 完成

#### 上下文语义理解与专家路由

- 【判断】AI或语义路由需要判断用户是在要求解释知识、深挖经历、适配简历、研究岗位、模拟面试还是复盘。
- 固定规则可以做初筛，但无法可靠理解“把上面的内容再改细一点”所指对象和目标。
- 输出会随当前岗位、项目、简历版本和历史对话变化。
- 错误判断可能把行业知识写成岗位话术、把团队成果写成个人贡献，或选择错误文档。

#### 经历事实深挖与证据边界判断

- 【事实】经历侦探的提示词要求识别背景、本人动作、协作边界、数字口径和证据缺口。
- 【判断】固定表单能收集字段，但不能判断叙述中的归因夸大、数字缺定义或关键决策缺失。
- 错误判断会污染简历、项目事实和面试答法。

#### 面试逐字稿还原与复盘

- 【事实】面试系统需要从杂乱逐字稿中还原问题、回答、追问、面试官意图、风险和下一次答法。
- 【判断】这依赖语义、上下文和对话顺序，不是关键词规则能够稳定完成。
- 输出随岗位 JD、实际回答和用户复盘变化。
- 错误判断会错误归因失败原因，并把错误训练方向传给后续模拟。

#### 多轮教练对话

- 【事实】代码存在面试题教练消息表和教练消息接口，并允许将某条教练建议应用到批改版本。
- 【判断】多轮对话的价值在于用户可以指出“这不是我的意思”“这个数字不对”“面试官其实在问另一件事”，AI需要重新理解而不是只改字段。

### AI 可以增强，但并非必要

#### GitHub 项目解析

- 不用 AI：用户手动填写项目名称、简介、技术栈和链接。
- AI 增强：从仓库或项目文档生成初稿，减少录入。
- 【判断】它不构成产品核心，错误时用户仍需手动校对。

#### 岗位画像和差距诊断

- 不用 AI：用户根据 JD 手动标注要求、证据和差距。
- AI 增强：自动拆解 JD、初步匹配经历并提示缺口。
- 【判断】真正价值取决于是否连接真实经历和后续行动，而不是生成一份漂亮分析。

#### 邮件归类

- 不用 AI：用规则、发件人和关键词进行归档。
- AI 增强：识别面试邀请、截止时间、下一步动作和岗位归属。
- 【判断】明确格式的邮件可以用普通规则处理，模糊邮件才需要模型。

#### 文档结构整理

- 不用 AI：用户手动编辑 Markdown。
- AI 增强：识别重复、缺失章节和更合适的层级。
- 【判断】保存、版本、路径和打开文档都不需要 AI。

### 普通软件能力

- 【事实】以下能力是普通软件能力：
  - 会话和任务存储；
  - 文件夹、列表和搜索；
  - 左右栏收放和拖动；
  - 文档路径展示；
  - 直达按钮；
  - 版本号和状态；
  - 待确认修改；
  - 勾选、拒绝和确认写入；
  - 执行日志展示；
  - 日历和提醒；
  - 邮件连接；
  - 专家配置和模型配置；
  - 外部 Agent 接入。
- 【判断】这些能力对产品可信度很重要，但不应包装成 AI 智能本身。

---

## 5. 人与 AI 如何共同工作

### 5.1 AI 提出什么

- 【事实】AI可以提出：
  - 主理专家与协作专家；
  - 项目事实追问；
  - 简历候选改写；
  - 知识文档修改；
  - 岗位研究和差距；
  - 面试题意图、问题诊断和下次答法；
  - 新建或修改文档的候选方案。

### 5.2 用户在哪一步判断、修改或确认

- 【事实】用户可以在聊天中纠正，也可以在 `proposed_changes` 层编辑、拒绝或确认。
- 【事实】正式写入通过 `/api/agent/tasks/{task_id}/apply`、知识 Agent apply、聊天动作 apply 或面试教练消息 apply 完成。
- 【判断】最关键的人工判断包括：
  - 事实是否真实；
  - 归因是否属于本人；
  - 文档范围是否正确；
  - 表达是否适合当前岗位；
  - 复盘结论是否符合面试现场。

### 5.3 一次纠正是否影响后续结果

- 【事实】面试题教练消息可以保存，并将 proposal 应用到当前批改版本。
- 【事实】Agent 候选可以被拒绝并记录理由；代码文案说明拒绝原因会回流给后续 Agent。
- 【事实】项目、知识、面试和岗位结果写入正式对象后，会成为后续上下文可读取的资料。
- 【判断】当前系统已经具备“纠正可以改变后续资料”的结构，但我没有完成对所有专家是否稳定消费这些纠正的端到端验证。

### 5.4 影响如何发生

```text
用户纠正
→ 保存为消息、候选编辑、拒绝原因或正式文档更新
→ 更新对应项目/知识/面试/岗位对象
→ 后续上下文构建读取新版本
→ 下一次回答或训练基于更新后的材料
```

### 5.5 用户不参与判断时产品是否成立

- 【判断】作为普通问答工具仍可工作，但作为职业事实系统不成立。
- 【判断】用户不参与时，系统无法可靠确认贡献边界、面试现场意图和写入范围；这些错误一旦进入简历和项目档案，会比一次聊天答错更严重。

### 5.6 人机分工

- 【判断】AI负责读取、联想、拆解、发现缺口和生成候选。
- 【判断】用户负责事实所有权、价值取舍、现场解释和正式写入。
- 【判断】普通软件负责保存对象、展示路径、管理版本和执行确认。

---

## 6. 与 Skill 或通用对话的关系

### 6.1 可以直接写成 Skill 的能力

- 根据 JD 拆解岗位要求；
- 根据项目材料生成追问；
- 检查简历 Bullet；
- 把知识解释成定义、机制、例子和边界；
- 从逐字稿生成一次性复盘；
- 生成模拟面试 Prompt；
- 从 GitHub README 生成项目简介。

这些都可以由一个带模板和步骤的 Skill 完成。

### 6.2 用 ChatGPT 或 Claude 对话即可完成的能力

- 对一段简历文字提出修改；
- 讨论一个项目怎么讲；
- 针对一份逐字稿做复盘；
- 模拟一轮面试；
- 解释某个专业知识；
- 生成一版自我介绍。

【判断】如果用户愿意每次手动提供完整材料，并自行保存结果，通用对话足以完成这些单次任务。

### 6.3 只保留 Skill 会明显退化的任务

#### 跨对象事实核对

- 退化步骤：Skill 生成内容前无法稳定知道当前投递简历、项目事实、岗位 JD 和历史反馈的版本关系。
- 结果：用户需要反复粘贴，且难以判断使用了哪一版。

#### 候选修改进入正式资料

- 退化步骤：Skill 可以输出 Markdown，但不能自动提供 Caddie 内的目标对象、当前版本、差异和确认写入流程。
- 结果：用户仍需手工复制，并可能写错位置。

#### 面试反馈回流

- 退化步骤：一次复盘可以生成，但难以把某道题的事实问题回流到项目、把岗位局部问题留在当前岗位、把通用表达问题变成长期训练规则。
- 结果：复盘成为孤立报告，下一次面试仍从零开始。

#### 多轮纠正后的后续消费

- 退化步骤：通用聊天可以记住当前线程，但不自然对应项目文档、面试题版本、岗位知识和候选写入状态。
- 结果：纠正停留在对话中，未必改变正式事实。

### 6.4 独立产品形态是否必要

- 【判断】如果 Caddie 只做“生成简历、生成复盘、模拟面试”，独立产品形态不必要，Skill 足够。
- 【判断】独立产品形态只有在以下链条成立时才必要：
  1. 用户职业材料被组织成明确对象；
  2. AI读取的是可见、可选择的上下文；
  3. 结果以候选修改进入对象；
  4. 用户确认事实和范围；
  5. 真实面试反馈能够改变后续项目、简历和训练。
- 【事实】当前代码已经有支持该链条的数据结构和页面。
- 【未知】该链条是否在真实用户使用中高频成立，尚无我能引用的统计证据。

---

## 7. 模块在整个产品中的位置

### 选择

- 【判断】统一 Agent 工作台：**支撑核心闭环的基础设施**。
- 【判断】文档候选与确认写入：**核心闭环的一部分**。
- 【判断】专家角色和手动选择：**已实现但尚未验证其必要程度**。
- 【判断】面试反馈回流：如果端到端成立，是**核心闭环的一部分**；当前成熟度应标记为**已实现但尚未充分验证**。
- 【判断】Codex 式视觉本身：**可以替换，不是产品本体**。

### 7.1 删除后核心价值是否成立

- 删除统一工作台视觉：核心资料系统仍可成立，但使用会重新碎片化。
- 删除候选修改、路径和确认：Caddie 会退化成多个生成器和聊天框，职业事实可信度明显下降。
- 删除面试反馈回流：项目、岗位和简历管理仍存在，但“真实外部结果反过来修正内部认知”的差异会消失。

### 7.2 上游依赖

- 岗位档案与 JD；
- 项目与经历事实；
- 简历版本；
- 知识文档；
- 面试逐字稿、问题和回答；
- 模型配置；
- 上下文构建与检索。

### 7.3 下游使用

- 简历候选修改；
- 项目事实文档；
- 项目追问题库；
- 岗位知识和面试准备文档；
- 面试复盘文档；
- 自我介绍和项目讲法；
- 模拟训练；
- 首页待处理动作；
- 外部 Agent 更新审核。

### 7.4 是否把真实反馈传回上游

- 【事实】面试复盘可以关联岗位和项目；页面文案明确写有“新追问回流到这里”和“局部反馈”。
- 【事实】复盘输出可以生成项目追问、后续生成约束和岗位知识文档。
- 【事实】面试题可以被标记用于准备，教练批改可以形成知识草稿。
- 【判断】这些是回流机制的代码证据。
- 【未知】我没有完整追踪一个真实面试从逐字稿到项目事实修正、再到下一版简历的生产数据。

---

## 8. 证据与成熟度

### 8.1 相关文件

- `static/index.html`
  - 全局工作台、项目/岗位/简历/知识 Agent 入口、首页、旧版面试兼容页面。
- `static/caddie-ui.css`
  - Codex 式工作台、侧栏、文档区、专家库、输入框和响应式布局。
- `static/interview-system.js`
  - 新版面试库、轮次工作区、题目详情、深度批改和教练交互。
- `static/interview-system.css`
  - 新版面试系统视觉与布局。
- `server.py`
  - 聊天、专家路由、Agent 任务、候选写入、面试拆解、教练消息和复盘接口。
- `db.py`
  - 专家定义、统一 Agent Runtime、候选修改和日历等对象。
- `interview_store.py`
  - 面试轮次、题目、教练版本和教练消息。
- `agent_runtime.py`
  - 专家分诊和主理专家提示词组装。
- `tests/test_interview_question_assets.py`
  - 面试题批改、教练消息、知识草稿和资产相关回归验证。
- `tests/test_agent_project_update.py`
  - 经历侦探生成项目追问与项目文档候选修改。
- `tests/test_agent_global_knowledge.py`
  - 全局知识 Agent 生成和确认。

### 8.2 关键代码和数据表

- `AGENT_EXPERT_SEEDS`：7 位专家的职责、工具、模型档案和允许动作。
- `agent_tasks`：任务对象和上下文绑定。
- `agent_runs`：实际运行、专家、模型和状态。
- `agent_events`：执行过程。
- `proposed_changes`：候选修改和确认状态。
- `knowledge_agent_runs` / `knowledge_agent_changes`：知识 Agent 兼容层。
- `interview_rounds`：真实面试轮次。
- `interview_question_coaching_versions`：面试题批改版本。
- `interview_question_coaching_messages`：多轮教练对话。

### 8.3 对应页面

- 全局 Caddie 工作区；
- 项目详情；
- 岗位档案；
- 简历版本；
- 知识文档；
- 面试与复盘；
- 首页“今天”；
- 邮箱助理；
- 设置中的外部 Agent 接入。

### 8.4 Git 提交

- 【事实】`376856f checkpoint Caddie agent workspace redesign`
  - 8 个文件；
  - 5873 行新增、401 行删除；
  - 包含 Agent 工作台和面试系统相关改动。
- 【事实】前序提交 `dbfe1fa 完善外部 Agent 协作与 Caddie Alpha 基础设施` 已存在，但我没有参与该提交过程，不能把它当作我的构建成果。

### 8.5 用户原话和需求记录

- “我觉得caddie对话框做的太丑了，你能不能完全复刻一个codex工作台”
- “右边的那个我觉得应该是和你一样的文档展示吧，包括输入agent执行我觉得应该像你一样”
- “我需要清晰的知道如果是文档修改，我需要知道修改的文档或者新建文档的路径，需要有一个直达的按钮”
- “专家团又去哪里了”
- “中间可以自由缩放，以及左边可以回收，看看codex怎么做的”
- “这个叉和上面的收放重复了，而且上面的收放太大了”
- “特别是面试复盘之类的我觉得还是用对话比较好”

### 8.6 真实数据

- 【事实】用户提供了真实操作截图和具体任务内容，证明工作台至少被用于项目整理、经历判断、知识查看和界面迭代。
- 【事实】仓库有本地数据库路径和相应表结构。
- 【未知】本轮未获准成功读取正式数据库，因此无法给出任务、面试轮次、候选修改或真实写入数量。
- 【未知】没有可引用的留存、完成率、写入确认率或复盘后面试改善数据。

### 8.7 已完成但没有使用证据

- 专家手动选择是否比自动分诊更常用；
- 外部 Agent 更新审核是否形成稳定使用习惯；
- 面试题教练多轮消息是否被真实用户持续使用；
- 拒绝原因是否稳定影响后续专家输出；
- 复盘结论是否实际进入下一轮简历修改；
- 右侧文档产出区是否显著减少寻找文件的时间。

### 8.8 仍停留在设想中的能力

- 所有 Agent 入口完全共享一套工作台组件；
- 任务型执行完成后无缝转入同一多轮对话；
- 面试复盘结果自动区分“当前面试局部反馈”和“长期项目认知”；
- 用真实行为数据验证专家团是否必要；
- 首页从“岗位推进面板”进一步转向“反馈驱动的职业资产工作台”。

---

## 额外重点：当前首页与产品中心

### 当前首页向用户传达的产品是什么

- 【事实】当前首页标题是“今天”，核心内容包括当前重点岗位、未来节点、待处理事项、重点岗位、日历和新增岗位。
- 【判断】它首先传达的是一个“求职推进与日程工作台”，接近求职 CRM 或个人求职任务管理器。
- 证据位置：`static/index.html` 中 `renderHome()`。

### 首页呈现的中心和产品真正的中心是否一致

- 【判断】如果把 Caddie 的差异理解为“真实经历—岗位—简历—面试反馈—资料修正”的循环，当前首页并未充分表达这个中心。
- 【事实】首页会读取岗位、日历、邮件提醒和外部 Agent 更新，但不会直接展示：
  - 最近一次真实反馈改变了什么；
  - 哪个项目事实被修正；
  - 哪版简历需要因面试反馈更新；
  - 哪个长期能力缺口正在跨岗位重复出现。
- 【判断】因此首页呈现中心与“反馈回流的职业认知系统”并不完全一致，更偏向推进管理。

### 哪些页面最能体现产品本体

- 【判断】最能体现本体的不是首页，而是以下页面之间的连续流程：
  1. 项目事实文档；
  2. 岗位档案及对应简历版本；
  3. 面试轮次与逐题复盘；
  4. 候选修改和确认写入；
  5. 下一轮模拟训练。
- 【事实】这些页面有明确的对象关联和上下游数据接口。

### 用户第一次进入时能否理解为什么需要 Caddie

- 【判断】当前不一定能。
- 新用户首先看到“今天、岗位、日历、待办”，容易理解为求职 CRM。
- 进入全局工作区后又容易理解为带知识库的聊天机器人。
- 进入知识文档后容易理解为个人知识库。
- 【判断】只有完整体验“真实面试反馈修改项目和下一轮准备”后，用户才可能理解独立产品必要性；首页目前没有把这条链路讲清楚。

### 容易造成误解的交互

#### 被理解成求职 CRM

- 首页强调岗位数、节点、日历和待处理；
- 投递看板和状态流转显眼；
- 反馈如何改变资料不显眼。

#### 被理解成知识库

- 知识树、目录、Markdown 文档和文档地图较完整；
- 如果 Agent 只负责整理文档，会接近带 AI 的知识库。

#### 被理解成聊天机器人

- 多个页面都有“打开 Agent”“AI 诊断”“知识专家”；
- 如果结果只显示为对话文字，没有路径和确认写入，就会像套壳聊天。

---

## 额外重点：面试在 Caddie 中的位置

### 面试是终点、训练场还是外部验证机制

- 【判断】它首先应当是**外部验证机制**，其次才是训练场，不应只是求职流程终点。
- 理由：真实面试会暴露内部资料无法证明的问题——项目是否可信、贡献边界是否清楚、岗位理解是否准确、表达是否经得住追问。
- 【事实】当前代码同时支持原始记录、复盘诊断、现场答法、项目讲法和模拟训练，说明它已经不只是保存面试结果。

### 一次真实面试产生的信息最终流向哪里

按当前代码可以流向：

- 面试轮次和逐字稿来源；
- 结构化面试问题；
- 每道题的原回答、面试官意图、问题和改进答法；
- 面试复盘文档；
- 面试总库；
- 岗位局部反馈；
- 项目追问；
- 后续生成约束；
- 知识缺口和知识草稿；
- 模拟训练上下文。

【未知】不是每次面试都会实际写入所有这些位置。

### 哪些反馈只服务当前面试

- 当前轮次的面试官身份、当场追问顺序、具体表达失误；
- 当前岗位局部的面试判断；
- 某一次回答的措辞和现场节奏；
- 本轮是否通过及阶段结果。

### 哪些反馈会改变长期项目认知

- 对本人贡献边界的质疑；
- 对关键数字口径的追问；
- 对项目目标用户和必要性的持续质疑；
- 对技术、流程或决策细节的事实缺口；
- 多场面试重复出现的同类问题；
- 能证明项目文档原有表述不准确的外部反馈。

【判断】只有这些内容经过用户确认后，才应回写项目事实或长期规则，不能把面试官的单次意见自动当成真相。

### 与市面复盘产品最实质的区别

- 【判断】不是“总结得更详细”，也不是“AI 自动评分”。
- 真正差异只可能是：复盘结论能定位到产生问题的上游对象，并改变后续使用的项目、简历、知识或岗位准备。
- 如果只是上传逐字稿、生成报告和练习题，通用对话和现有复盘工具都能完成。

### 如果复盘结果没有回流

- 【判断】Caddie 仍剩下岗位管理、项目知识库、简历版本、AI 对话和模拟面试。
- 【判断】但它与求职 CRM、AI 知识库和面试复盘工具的差异会显著缩小。
- 【判断】“真实外部反馈修正内部职业资料”是最值得验证、也最不能仅靠页面文案声称成立的部分。

---

## 9. 最重要的结论

### 1. 最有价值的产品判断是什么

【判断】Agent 产出不能停留在回答。它必须明确自己读取了什么、准备修改什么、写到哪里，并由用户确认后成为下一轮可用的资料。

### 2. 用户做了哪一个我作为 Agent 不会自然做出的决定

【事实】用户拒绝把面试复盘简单降级为一次性任务，要求保留多轮对话调整。  
【判断】Agent 容易追求流程整齐，把“拆解—输出—保存”做成线性任务；用户指出真正困难发生在对 AI 判断的持续纠正，而不是第一次生成。

### 3. 哪个能力看起来很炫，但不应该成为主要亮点

【判断】七位人格化专家和自动分诊看起来很炫，但不应成为主要亮点。若上下文、事实边界、候选写入和反馈回流不成立，专家团只是更复杂的提示词选择器。

### 4. 哪一个真实流程最值得在三分钟 Case Study 中展示

【判断】最值得展示的是：

```text
导入一次真实面试逐字稿
→ 还原问题、回答和追问
→ 用户纠正 AI 对面试官意图的误判
→ 将确认后的事实问题回流到项目
→ 将岗位局部问题保留在当前岗位
→ 更新下一轮准备或简历候选
→ 用修正后的材料进行针对性模拟
```

这条流程同时展示外部验证、人工判断、对象写入和下一轮消费，而不是只展示生成文案。

### 5. 关于 Caddie，我最没有资格下结论的部分是什么

【未知】我最没有资格判断 Caddie 整体是否已经找到产品市场匹配、用户是否愿意长期维护职业资料，以及面试反馈回流是否在真实使用中产生了可衡量结果。我参与的是一个重设计和基础能力收敛阶段，不代表全部构建历史，也没有完整用户研究和使用数据。

