# Caddie 模块构建证词：岗位工作台与面试准备闭环（Codex 交互重构阶段）

> 证词日期：2026-07-27  
> 证词范围：只覆盖我在本轮连续对话中亲自参与、实际查看，或能由当前仓库文件证明的部分。  
> 不覆盖：Caddie 的完整历史、全部 Agent、商业定位、用户规模或我未核验的模块效果。

## 证词边界

### 我负责或深度参与过哪些模块

- 【事实】求职工作台的看板信息结构：岗位阶段、紧凑卡片、未通过岗位收纳、状态推进与停滞提醒。
- 【事实】岗位档案的四个页签及其关系：概览、文件与版本、准备知识、面试与复盘。
- 【事实】面试轮次的准备与复盘交互：每轮使用多篇文档、继承上一轮但不污染原文、复盘行动经用户采纳后进入后续准备。
- 【事实】一级导航“面试与复盘”与岗位内“面试与复盘”的职责区分。
- 【事实】首页“今天”及全局顶栏：折叠搜索、上下文入口、常用导航、返回路径。
- 【事实】求职计划模式的需求研究和 PRD：秋招、春招、提前批、日常实习等模式，以及投递、进程和复盘提醒。
- 【判断】这些模块共同涉及的是“从岗位进入准备，再让真实面试反馈回流”的局部闭环；我没有资格据此代表 Caddie 全部产品。

### 大致参与时间或阶段

- 【事实】本证词对应 2026-07-26 至 2026-07-27 的连续交互重构阶段。
- 【事实】我介入时，仓库中已经存在岗位、简历、知识、面试、邮箱、Agent 工作区等代码。我不是这些模块的从零创建者。
- 【判断】我参与的是已有功能较多、但页面层级和真实使用流程尚未收敛的阶段。

### 我直接修改或查看过的文件、页面、对象和功能

直接修改或在本轮实现中反复核验的主要文件：

- `static/index.html`
- `static/caddie-ui.css`
- `server.py`
- `db.py`
- `interview_store.py`
- `static/interview-system.js`
- `static/interview-system.css`
- `tests/test_interview_question_assets.py`

直接编写的需求文件：

- `docs/product/求职计划模式-需求调研报告-2026-07-27.md`
- `docs/product/PRD-求职计划与提醒系统-v0.1.md`
- `docs/product/求职计划模式-实施路线与原型说明.md`

直接查看的相关设计文件：

- `docs/SPEC-面试与复盘模块.md`
- `docs/SPEC-面试与复盘模块V2-岗位考试系统.md`
- `docs/SPEC-作品集与求职工作台.md`
- `docs/MEMORY-产品决策.md`
- `docs/interview-rebuild/12-Agent2-面试轮次与准备继承.md`

直接查看的页面：

- 首页“今天”
- 求职工作台表格和看板
- 岗位档案四个页签
- 岗位内的面试轮次页
- 一级导航“面试与复盘”
- Caddie 工作区
- 全局顶栏与返回导航

直接核验的数据对象：

- `job_tracks`
- `applications`
- `agent_tasks`
- `proposed_changes`
- `job_opportunities`
- `opportunity_plan_items`
- `documents`
- `document_versions`
- `document_links`
- `interview_rounds`
- `round_document_links`
- `transcript_sources`
- `review_actions`

### 哪些是亲自知道的，哪些只是推测

- 【事实】我亲自知道用户多次否定了“一个大页面装完”“一个编辑器装完”“加一个 Agent 框”的处理方式。
- 【事实】我亲自知道用户把“可编辑的多篇文档、轮次继承但相互隔离、真实反馈回流”视为面试模块的关键。
- 【事实】我亲自核验了轮次文档、版本、来源引用、继承接口和复盘行动采纳接口的代码。
- 【事实】我亲自核验了首页、求职看板、岗位四页签和全局顶栏的当前实现。
- 【判断】我认为岗位档案和面试闭环比纯聊天入口更接近本轮用户表达的产品中心。
- 【假设】这套结构会提升长期复用和面试准备质量；我没有可用性实验或长期留存数据。
- 【未知】我不知道其他用户是否以相同方式准备面试，也不知道当前所有代码是否都被稳定使用。

---

## 1. 我接到的原始任务

### 1.1 用户最初要求我解决什么问题

- 【事实】用户最初要求重新设计“面试与复盘”页面，因为现有页面不好用，目标是帮助用户管理面试并完成面试复盘。
- 【事实】随后任务扩大到求职工作台入口、岗位看板、首页、顶栏导航和求职计划提醒。
- 【事实】用户要求每次实施前先确认设计；在本轮后半段，用户明确授权先修改并看效果。

### 1.2 用户提供了哪些背景、案例或材料

- 【事实】用户提供了多张本地 Caddie 截图，并用红框指出：
  - 岗位详情页右侧无价值的信息栏；
  - 面试准备与岗位准备重复；
  - 面试与复盘页面内容太多但使用路径不清；
  - 看板卡片过宽，未通过岗位占据大量空间；
  - 顶栏过厚，搜索低频却长期占位；
  - 返回路径笨重或不好看。
- 【事实】用户口述了岗位档案四块内容：
  1. 概览：JD 关键信息与个人经历、能力维度的匹配、分数和缺口；
  2. 文件与版本：管理 Word、PDF 和本地文件位置，并方便在 WPS 修改；
  3. 准备知识：以多篇文档呈现，面试时可以快速查找和编辑；
  4. 面试与复盘：每轮独立准备、继承上一轮、复盘后反馈到总文档和经历知识。
- 【事实】用户提出可以像 Obsidian 一样展示一个岗位的知识树或知识脉络图。
- 【事实】用户提出看板应支持秋招、春招、提前批、日常等模式，并对一周未更新的岗位和一周无投递进行提醒。

### 1.3 我最开始如何理解需求

- 【判断】我最开始把它理解为对单个“面试与复盘”页做更清晰的步骤条、轮次卡和信息卡。
- 【判断】这个理解过于页面化，没有先区分“岗位级长期准备”和“每轮面试增量准备”，也没有理解文档是用户面试现场的操作界面，而不只是 AI 输出容器。

### 1.4 我最初提出或实现了什么方案

- 【事实】最初方案将单轮面试拆为准备、提交资料、确认答卷、复盘与行动、结果记录等步骤。
- 【事实】页面仍保留较大的单轮准备编辑区、引用区和右侧岗位信息区。
- 【事实】随后才逐步转为：
  - 岗位级四页签；
  - 多篇准备文档；
  - 轮次之间的独立复制与来源记录；
  - 一级面试入口负责跨岗位总览；
  - 岗位内入口负责具体轮次生命周期。

---

## 2. 用户如何纠正我

### 纠正一：预览图不能脱离用户当前真实页面

- 你的原方案：先展示一张我理解中的入口或页面预览。
- 用户为什么不满意：用户指出“你的怎么和我的不一样”，说明预览没有建立在其当前页面结构和数据状态上。
- 用户实际想要的：直接在当前 Caddie 中修改，让设计与真实导航、数据和功能保持一致。
- 最终如何修改：后续设计均围绕现有 `static/index.html` 页面和真实岗位档案结构实施，不再把独立视觉稿当成已实现效果。
- 这次修改反映了什么产品判断：已有产品重构必须以真实状态和真实数据为约束；脱离代码的漂亮预览不能证明可用。
- 证据位置：本轮对话原话“你的怎么和我的不一样”；`static/index.html`。

### 纠正二：右侧岗位档案与 Agent 工作台卡片没有必要

- 你的原方案：在岗位的面试页右侧固定展示“岗位档案”统计和“Agent 工作台”入口。
- 用户为什么不满意：它重复了页面已有上下文，占用主工作区宽度，妨碍阅读和编辑。
- 用户实际想要的：删除低价值固定侧栏，把空间还给轮次和文档。
- 最终如何修改：岗位详情主内容改为完整宽度，Agent 作为按需入口而不是固定说明卡。
- 这次修改反映了什么产品判断：持续占据屏幕的内容必须支持当前任务；上下文存在不等于必须重复展示。
- 证据位置：用户红框截图；`static/index.html` 的 `renderTrackDetail()`。

### 纠正三：“面试准备”不能与“岗位准备”重复

- 你的原方案：把“本轮准备”作为单独的大编辑内容，与岗位准备知识并列。
- 用户为什么不满意：准备内容本来就属于岗位准备；再建一个孤立区会形成重复和分叉。
- 用户实际想要的：岗位准备是基础；每轮面试在其上继承、调整重点和补充细节。
- 最终如何修改：把准备建模为岗位范围和轮次范围的文档，轮次文档带来源、版本和继承关系。
- 这次修改反映了什么产品判断：轮次准备应是岗位知识的增量分支，不是另起一套互不相干的内容。
- 证据位置：`interview_store.py` 的 `documents`、`document_versions`、`round_document_links`、`interview_rounds.inherited_from_round_id`。

### 纠正四：不能把全部准备放进一个编辑器

- 你的原方案：单轮页面提供一个主要编辑器，承载本轮目标、保留内容和新增重点。
- 用户为什么不满意：面试时无法快速找到某个问题、项目或知识点；内容越多，单页越难用。
- 用户实际想要的：像文档库一样拥有多篇可编辑文档，能从目录或知识树快速进入准确位置。
- 最终如何修改：每轮至少有准备文档、结构化逐字稿和面试答卷，并可关联其他参考文档；页面支持文档列表和目录。
- 这次修改反映了什么产品判断：文档不仅是存储格式，也是高压面试场景中的检索和操作界面。
- 证据位置：`interview_store.py::ensure_round_documents()`；`static/index.html` 的知识工作区和岗位准备页。

### 纠正五：继承不能污染上一轮

- 你的原方案：早期只表达“继承上一轮有效内容”，没有明确引用、复制和版本边界。
- 用户为什么不满意：如果直接修改上一轮文档，历史准备会被污染，用户无法回看当时状态。
- 用户实际想要的：把上一轮已有内容带进本轮，建立本轮独立可编辑版本，同时保留来源。
- 最终如何修改：`POST /api/v2/interview-rounds/{rid}/inherit` 会复制符合类型的上一轮文档，写入 `source_ref_id` 和继承元数据，并建立本轮独立文档。
- 这次修改反映了什么产品判断：继承的本质是有来源的分支，而不是覆盖或共享同一正文。
- 证据位置：`server.py::inherit_round_context_v2()`；`interview_store.py::link_round_document()`。

### 纠正六：复盘不是结束页，而要反哺后续准备和经历知识

- 你的原方案：把复盘结果主要展示为单场总结、评分和下一步建议。
- 用户为什么不满意：如果结论只留在复盘页，就不能改善下一轮，也不能丰富项目、经历或总准备文档。
- 用户实际想要的：每轮总结考察重点、薄弱点和有效回答；多轮后再归纳岗位重点，并提出对项目文档和经历知识库的补充。
- 最终如何修改：复盘行动成为候选，用户点击采纳后才追加到本轮后续计划或准备文档；正式事实和知识修改仍走候选确认。
- 这次修改反映了什么产品判断：面试价值不只是记录结果，而是把外部真实反馈转成下一次可使用的改变。
- 证据位置：`interview_store.py::apply_review_action()`；`server.py::apply_review_action_v2()`；`db.py` 的 `proposed_changes`。

### 纠正七：岗位档案的四个模块各有真实职责

- 你的原方案：概览、文件、准备、复盘虽已存在，但内容边界混杂；文件页还有“成果展示”等附加内容。
- 用户为什么不满意：缺少 JD 与经历能力匹配；文件页偏离简历管理；准备和复盘之间没有清晰的长期关系。
- 用户实际想要的：
  - 概览负责 JD 提炼、证据匹配、能力维度和缺口；
  - 文件页负责本地 Word/PDF、版本与打开修改；
  - 准备页负责多篇可检索文档；
  - 面试页负责轮次、答卷、复盘和回流。
- 最终如何修改：页面继续保留四页签，但后续实现按上述职责收敛；并删除或弱化与当前任务无关的成果展示。
- 这次修改反映了什么产品判断：信息架构应按用户任务划分，而不是按数据库对象或已有功能堆叠。
- 证据位置：用户长段口述；`static/index.html` 中 `tabDefs` 与 `renderTrackDetail()`。

### 纠正八：一级“面试与复盘”不是岗位页的复制品

- 你的原方案：主要优化了岗位内页，左侧一级入口没有同步解决。
- 用户为什么不满意：用户明确说“我是说左边菜单栏那个总的”，说明一级入口承担不同任务。
- 用户实际想要的：一级入口管理跨岗位的待准备、待复盘、能力弱点和可复用规律；岗位内页管理具体岗位的轮次。
- 最终如何修改：一级页加入系统地图、跨岗位入口和状态总览；岗位内页保留轮次生命周期。
- 这次修改反映了什么产品判断：岗位线记录事实发生的位置，跨岗位入口负责经验复用，两者不能混成一个页面。
- 证据位置：`static/index.html` 的一级 `renderInterview()` 区域；`docs/SPEC-面试与复盘模块.md`。

### 纠正九：求职看板不应让低价值状态占据最大空间

- 你的原方案：每个状态都是同宽列，每张卡展示公司、岗位、标签、日期和多个按钮；“未通过”17条也完整展开。
- 用户为什么不满意：卡片过宽，空列浪费空间；未通过岗位的主要剩余价值只是复用准备资料。
- 用户实际想要的：紧凑动态看板；卡片只显示公司和岗位；箭头推进、圆圈终止；未通过默认收起且只展示近两周。
- 最终如何修改：看板改为紧凑卡片和动态状态操作，结束态弱化并收纳，表格与状态数据共用事实源。
- 这次修改反映了什么产品判断：看板应帮助当前行动，而不是平等陈列所有历史记录。
- 证据位置：用户两张看板截图；`static/index.html` 的求职工作台看板渲染；`static/caddie-ui.css` 的看板样式。

### 纠正十：看板颜色必须表达求职批次和 Offer 含义

- 你的原方案：主要按流程状态着色，卡片信息较多。
- 用户为什么不满意：用户无法快速区分秋招、提前批、日常等不同节奏，也无法区分“收到但未接”和“已接受”。
- 用户实际想要的：批次用颜色编码；Offer 的待决定和已接受使用不同颜色。
- 最终如何修改：批次、状态和 Offer 子状态进入卡片视觉与筛选逻辑；但完整模式系统仍未落地。
- 这次修改反映了什么产品判断：颜色应承载会改变用户决策的语义，而不是装饰。
- 证据位置：用户原话；`static/index.html` 的看板卡片与状态标签。

### 纠正十一：提醒不是红点，而要形成求职模式和任务

- 你的原方案：在看板上增加“7天未更新”的提示。
- 用户为什么不满意：单个提示不能帮助用户准备秋招，也不能回答“本周该投多少、哪些进程要确认”。
- 用户实际想要的：开启秋招、春招、提前批或日常模式；设置周目标；一周无投递、岗位停滞、面试临近和待复盘时生成可处理任务。
- 最终如何修改：先完成需求调研、PRD 和实施路线，没有直接进入开发。
- 这次修改反映了什么产品判断：提醒必须连接目标、触发原因和可执行动作，不能只制造焦虑。
- 证据位置：`docs/product/PRD-求职计划与提醒系统-v0.1.md`，其状态明确为“等待产品确认，不进入开发”。

### 纠正十二：全局搜索低频，不应长期占据厚顶栏

- 你的原方案：顶部长期展示大搜索框、消息、问 Caddie 和本地运行信息。
- 用户为什么不满意：顶栏太厚、搜索不常用、主内容被压低。
- 用户实际想要的：不用时完全收起；保留返回、搜索和少量常用能力。
- 最终如何修改：顶栏压缩为约 40px；搜索改为图标触发并支持 `⌘K`，展开后可关闭。
- 这次修改反映了什么产品判断：全局工具应按需出现，把稳定空间留给当前任务。
- 证据位置：`static/index.html` 的 `topbar`、`openGlobalSearch()`；`static/caddie-ui.css` 的 `.topbar` 和 `.search.expanded`。

### 纠正十三：顶栏不能只变空，还要帮助用户到达目标

- 你的原方案：压缩后主要留下搜索、Caddie 和更多菜单。
- 用户为什么不满意：顶部显得空，而且仍“不方便用户到想去的地方”。
- 用户实际想要的：显示当前位置，并提供今天、新建、待办、日历等高频目的地。
- 最终如何修改：增加上下文区域和快捷入口，根据岗位、公司或普通页面显示不同位置。
- 这次修改反映了什么产品判断：导航价值不是塞满常用功能，而是让用户理解“我在哪里、下一步能去哪”。
- 证据位置：`static/index.html::renderTopbarContext()`、`topbarViewLabel()`。

### 纠正十四：返回路径技术可用，但视觉仍不成立

- 你的原方案：在顶栏和岗位页同时增加“求职工作台 › 公司 › 岗位档案”式面包屑。
- 用户为什么不满意：用户最后明确说“这样做不好看，你重新弄一下”。
- 用户实际想要的：更轻、更少重复、仍能可靠返回上一级。
- 最终如何修改：截至本证词生成时尚未完成新一轮视觉返工；当前代码仍保留被否定的版本。
- 这次修改反映了什么产品判断：导航正确不等于交互完成；重复路径和过强层级会增加噪声。
- 证据位置：本轮最后一次界面反馈；`static/index.html::renderTopbarContext()` 和岗位页 `track-file-breadcrumb`。

### 关于“AI不能编造或擅自修改”

- 【事实】用户坚持 AI 可以给出已有准备内容和建议，但用户必须能在其上修改；上一轮内容不得被污染。
- 【事实】当前代码将 Agent 变更放入 `proposed_changes`，存在编辑、拒绝和显式应用接口。
- 【事实】面试复盘规范写明：AI 不直接改正式库，只生成候选，用户确认后才写入正式对象。
- 【判断】这不是附加的安全提示，而是产品的内容所有权边界：AI负责理解和提议，用户负责确认事实与正式版本。
- 证据位置：`db.py`、`server.py` 的 proposed change / apply 接口；`docs/SPEC-面试与复盘模块.md`。

---

## 3. 模块解决的真实问题

### 3.1 服务的具体用户任务

- 【判断】用户任务不是“记录我投了哪些岗位”，而是围绕一个具体岗位持续完成：
  1. 理解 JD 要求；
  2. 找到自己的相关经历和证据；
  3. 维护该岗位使用的简历版本；
  4. 建立可在面试现场快速查找的准备文档；
  5. 针对每一轮调整重点；
  6. 面试后还原问题和回答；
  7. 把确认后的反馈用于下一轮及长期经历资料。

### 3.2 没有这个模块时如何完成

- 【判断】用户需要在本地 Word/PDF、WPS、零散笔记、聊天记录、日历和表格之间手工切换。
- 【事实】用户明确提到本地 Word、PDF、WPS 和多份准备资料，说明 Caddie 并不取代这些编辑工具，而是要把路径、版本和岗位关系组织起来。

### 3.3 原方法最困难的地方

- 【判断】最困难的不是少写一段文字，而是上下文断裂：
  - 不知道当前岗位到底缺什么；
  - 不知道应打开哪份简历和哪篇准备文档；
  - 上一轮有效内容难以复用，直接复制又失去来源；
  - 面试反馈只留在聊天总结中，不能改变下一轮行为；
  - AI 生成内容容易与用户事实混在一起。

### 3.4 模块改变的是什么

- 【判断】它同时改变效率、质量、认知和行为，但优先级不同：
  - **行为**：把“面试后有空再复盘”变成有轮次、有材料、有待确认行动的流程。
  - **认知**：把模糊的“我适不适合”拆成 JD 要求、个人证据、能力维度和未知。
  - **质量**：让准备和复盘引用具体文档、回答和面试信号，而不是泛泛建议。
  - **效率**：减少寻找文件、复制上一轮内容和反复向 AI 补上下文的成本。

### 3.5 使用前后的差异

- 【判断】使用前：岗位是看板里的一张卡，准备资料散落，面试结束后得到一份孤立总结。
- 【判断】使用后：岗位是一条有上下文的工作线；每轮面试拥有独立文档分支；复盘建议经确认后进入下一轮计划；历史状态仍可回看。
- 【假设】“使用后”描述的是代码和设计支持的目标状态，不等于已由长期用户验证。

### 3.6 真实使用案例

- 【事实】2026-07-27 的首页截图显示真实岗位“国泰海通 · 资本市场实习生”，下一轮为业务一面，时间为次日 09:00；同一首页还提示其他岗位需要补充面试时间或下一步跟进。
- 【事实】输入包括岗位记录、面试日期、岗位准备资料和待办状态。
- 【事实】用户可从首页打开岗位，再进入准备知识或面试轮次；当前代码支持建立本轮文档、继承上一轮、添加逐字稿、生成答卷与复盘行动。
- 【事实】复盘行动只有点击采纳后才追加到后续训练或准备文档。
- 【未知】我没有证据证明这个具体“国泰海通”案例已经完整跑完“真实面试结果 → 多轮归纳 → 修改经历知识库”的全链路，因此不能把后半段写成已发生事实。

---

## 4. AI 在其中的作用

### 必须由 AI 完成

#### JD 与个人经历的语义匹配

- 【判断】AI需要理解 JD 中隐含的职责、能力和证据要求，并把它们与用户经历中的行为、贡献边界和结果对齐。
- 【判断】固定关键词只能找到词面重合，无法可靠判断“做过类似事情”与“只是出现同一个词”的区别。
- 【判断】输出会随具体 JD、选中的经历版本和已确认事实变化。
- 【判断】错误会把不存在的能力当成证据，或把真正缺口掩盖成高匹配分。

#### 从噪声逐字稿恢复面试答卷

- 【事实】当前面试结构包含参与者、原始片段、问题、回答、追问、现场信号、置信度和确认状态。
- 【判断】AI需要判断说话人、问题边界、追问关系、候选人实际回答及面试官信号；固定规则无法处理口语、省略和多人对话。
- 【判断】错误会把面试官的话算作用户回答，或对不存在的回答进行评分。

#### 基于证据生成复盘判断和候选行动

- 【判断】AI需要结合 JD、准备文档、逐字稿、答卷和真实结果，区分亮点、风险、不确定性及下一步。
- 【判断】输出必须随岗位和本轮材料变化，不能套通用模板。
- 【判断】错误判断会误导下一轮准备，严重时还可能把口误写成长期职业事实，因此只能先生成候选。

#### 跨轮次提炼稳定规律

- 【判断】多轮内容中哪些是稳定考察重点、哪些只是一次追问，需要语义归纳而不是简单计数。
- 【判断】这一能力只有在样本和来源足够时才成立；单次结果不能机械升级为长期规则。

### AI 可以增强，但并非必要

- 【判断】准备文档初稿：不用 AI，用户可手工建立目录和内容；AI改善起稿和复用速度。
- 【判断】知识树分类：不用 AI，用户可手工建文件夹和链接；AI帮助推荐关系和发现重复。
- 【判断】任务优先级：不用 AI，可按截止时间和固定规则排序；AI可综合岗位价值、准备缺口和用户节奏提出建议。
- 【判断】周复盘：不用 AI，可用固定问题表单；AI可把一周记录归纳为候选调整。
- 【判断】文档检索：全文搜索可以不用 AI；AI可处理“我上次怎么解释这个项目”这类语义查询。

### 普通软件能力

- 【事实】岗位、投递和阶段的增删改查。
- 【事实】表格、看板、筛选、折叠和颜色编码。
- 【事实】Word/PDF 路径关联、版本记录、打开本地文件。
- 【事实】文档存储、版本、来源链接、目录和编辑。
- 【事实】轮次状态、日历、截止时间和停滞天数。
- 【事实】搜索、返回、面包屑和侧栏导航。
- 【事实】用户确认、拒绝、编辑候选修改。
- 【判断】这些能力重要，但不应被包装成 AI 亮点。

---

## 5. 人与 AI 如何共同工作

1. **AI提出什么？**
   - 【事实】AI可以提出 JD 画像、匹配缺口、准备文档草稿、逐字稿结构、答卷复原、复盘结论和后续行动。

2. **用户在哪一步判断、修改或确认？**
   - 【事实】用户选择和编辑准备文档；纠正说话人和答案；采纳或拒绝复盘行动；确认候选修改后才进入正式事实或知识对象。

3. **用户的一次纠正是否影响后续结果？**
   - 【事实】确认后的参与者、问题标签、文档版本和反馈对象可以被后续轮次或 Agent 上下文读取。

4. **影响如何发生？**
   - 【事实】通过文档版本、`source_ref_id`、`inherited_from_round_id`、confirmed 状态、`proposed_changes` 和已采纳行动发生，不只依赖聊天历史。

5. **用户不参与判断，产品是否仍成立？**
   - 【判断】岗位管理、文件打开、提醒和文档阅读仍成立；涉及个人事实、能力判断和正式回流的闭环不成立。
   - 【判断】如果完全自动写入，产品会积累无法区分来源的错误内容。

6. **体现了怎样的人机分工？**
   - 【判断】软件保存状态和来源；AI负责理解、比较、归纳和提出候选；用户负责真实性、取舍和正式版本。

---

## 6. 与 Skill 或通用对话的关系

### 6.1 哪些能力可以直接写成 Skill

- 【判断】“输入 JD 和经历，输出证据化匹配表”可以写成 Skill。
- 【判断】“输入逐字稿，恢复题目、回答和追问”可以写成 Skill。
- 【判断】“根据上一轮复盘生成下一轮准备建议”可以写成 Skill。
- 【判断】“按固定五问生成周复盘”可以写成 Skill。

### 6.2 哪些用 ChatGPT 或 Claude 对话就能完成

- 【判断】单次 JD 解读、一次简历建议、一份准备清单、一次逐字稿总结和一次模拟面试都可以通过通用对话完成。
- 【判断】前提是用户愿意手工粘贴所有资料，并自己管理版本和写回位置。

### 6.3 只保留 Skill 会在哪些任务上退化

- 【判断】选择“这次到底使用哪份本地简历、哪版项目事实和哪轮准备文档”时退化。
- 【判断】继承上一轮但保留独立版本、来源和历史状态时退化。
- 【判断】将用户确认后的结论写回正确岗位、文档或经历对象时退化。
- 【判断】面试现场在多篇文档间快速定位时退化。
- 【判断】真实结果到来后校准旧预测和后续规则时退化。

### 6.4 退化发生在具体哪个步骤

- 【判断】不是发生在“生成文字”这一步，而是发生在生成前的上下文选择、生成后的人工修订、正式应用、版本追踪和下一轮复用。

### 6.5 独立产品形态是否必要

- 【判断】对单次求职建议不必要，通用对话足够。
- 【判断】对“同一用户、多个岗位、多份本地文档、多轮面试、需要非破坏性回流”的任务有必要，因为产品承担了可视化选择、来源约束和状态同步。
- 【事实】用户连续要求多篇文档、轮次继承、不污染历史、路径可找和可编辑，这些要求不是单个提示词能够稳定提供的界面能力。

### 6.6 是否整个模块都不需要做成产品

- 【判断】面试总结生成器本身不值得单独做成产品；它容易被通用模型替代。
- 【判断】岗位上下文、可操作文档、轮次分支和经确认回流组成的闭环，才有独立产品形态的理由。

---

## 7. 模块在整个产品中的位置

### 位置选择

- 【判断】**岗位工作台与面试准备/复盘：核心闭环的一部分。**
- 【判断】**首页和顶栏：支撑基础设施。**
- 【事实】**求职计划模式：实验性能力，已有 PRD，尚未实现。**
- 【判断】**动态看板：重要的普通软件能力，不是产品核心本身。**

### 7.1 删除后核心用户价值是否还成立

- 【判断】删除看板，Caddie 仍可做职业文档和面试分析，但用户难以管理并行岗位。
- 【判断】删除面试回流，Caddie 仍可做岗位 CRM 和知识库，但“从真实外部反馈改进职业材料”的价值会明显断裂。
- 【判断】删除首页或顶栏不会删除底层价值，但会降低首次理解和日常进入效率。
- 【判断】删除尚未实现的求职计划模式，不影响当前产品运行。

### 7.2 上游依赖

- 【事实】岗位工作台依赖 `job_tracks`、`applications` 和机会数据。
- 【事实】岗位概览依赖 JD、经历、项目事实和简历版本。
- 【事实】面试轮次依赖具体岗位及其准备文档。
- 【事实】AI 复盘依赖逐字稿、参与者确认、答卷结构和岗位上下文。

### 7.3 下游使用

- 【事实】轮次继承会读取上一轮文档。
- 【事实】采纳的复盘行动进入准备或后续训练文档。
- 【事实】确认后的候选修改可进入知识、项目、岗位或职业事实对象。
- 【判断】简历和长期经历库应使用这些确认结果，但我没有证据证明所有下游页面已完整消费。

### 7.4 是否把真实反馈传回上游

- 【事实】代码支持复盘行动采纳和 proposed-change 确认。
- 【事实】设计规范要求真实结果用于校准预测和类别规则。
- 【未知】我没有长期真实数据证明多轮面试结果已经稳定改变上游项目文档或提高后续面试表现。

---

## 8. 证据与成熟度

### 8.1 相关文件路径

- 【事实】`static/index.html`：首页、求职工作台、岗位四页签、一级面试页、全局顶栏。
- 【事实】`static/caddie-ui.css`：顶栏、岗位页、看板、文档工作区样式。
- 【事实】`server.py`：轮次继承、逐字稿、复盘行动采纳、候选修改应用等接口。
- 【事实】`interview_store.py`：面试文档、轮次、继承关系、答卷和复盘行动存储。
- 【事实】`db.py`：岗位、投递、Agent 任务和 proposed changes。
- 【事实】`docs/SPEC-面试与复盘模块.md`：面试模块设计、证据要求、候选确认和回流原则。
- 【事实】`docs/product/PRD-求职计划与提醒系统-v0.1.md`：求职模式、周计划和提醒规则；状态为待确认。

### 8.2 关键代码或数据表

- 【事实】`interview_rounds.inherited_from_round_id`：轮次来源。
- 【事实】`documents` / `document_versions` / `document_links`：可编辑文档及版本、引用。
- 【事实】`round_document_links`：轮次与多篇文档的关系。
- 【事实】`server.py::inherit_round_context_v2()`：复制上一轮文档，建立本轮独立版本。
- 【事实】`interview_store.py::ensure_round_documents()`：创建本轮准备、结构化逐字稿和答卷。
- 【事实】`interview_store.py::apply_review_action()`：用户采纳后追加到后续训练或准备文档。
- 【事实】`proposed_changes`：AI 候选修改和确认状态。
- 【事实】`opportunity_plan_items`：现有机会级计划项。
- 【事实】完整 campaign 模式所需的 `job_campaigns`、`campaign_tasks` 只在 PRD 中提出，未核验到当前实现。

### 8.3 对应页面

- 【事实】首页“今天”。
- 【事实】求职工作台看板与表格。
- 【事实】岗位档案：概览、文件与版本、准备知识、面试与复盘。
- 【事实】一级导航“面试与复盘”。
- 【事实】Caddie 工作区。

### 8.4 Git 提交记录

仓库中与这些模块相关的提交包括：

- 【事实】`376856f checkpoint Caddie agent workspace redesign`
- 【事实】`7ae99a7 按证据顺序稳定面试问题重建`
- 【事实】`bebedf7 补充面试模型离线验证样例`
- 【事实】`9e8abd8 重构求职工作台并新增岗位简历版本管理`
- 【事实】`25a097e 求职目标工作台：投递关联 + 两栏布局 + 目标对话台`
- 【事实】`e3f89c1 新增作品集、求职目标工作台、面试关注点自迭代；跨平台支持`
- 【未知】除本轮直接参与内容外，我没有足够证据把上述历史提交全部归因于我。

### 8.5 用户原话或需求记录

- 【事实】“准备这个一定是以一系列文档的形式准备，用户可以自行编辑。”
- 【事实】“每一轮面试都准备一系列文档，但是不是孤立准备，而是有继承的准备。”
- 【事实】“我又不希望就是污染我上一轮的东西。”
- 【事实】“你甚至可以想 Obsidian 那样做一个岗位的知识脉络图。”
- 【事实】“用户第一是原则，好用是第一性原则。”
- 【事实】“对于整个没有通过的，收起来，可以展开。”
- 【事实】“假如有一个节点的岗位很久没更新……需要有一个提醒机制。”
- 【事实】“这样做不好看，你重新弄一下。”

### 8.6 真实数据

- 【事实】本轮截图中的首页存在真实岗位、轮次时间、待补信息和岗位状态，不是纯空白演示。
- 【事实】截图中曾显示 3 条岗位在推进、1 个未来节点、2 项待处理；这是 2026-07-27 的界面快照，不代表长期规模。
- 【事实】求职看板截图曾显示 22 条岗位，其中 17 条未通过；这直接触发了对结束态收纳的需求。

### 8.7 已完成但没有使用证据的能力

- 【事实】轮次独立文档继承接口已经存在。
- 【事实】复盘行动显式采纳接口已经存在。
- 【事实】全局顶栏折叠搜索和快捷入口已经存在。
- 【事实】一级面试页和岗位内面试页已经区分。
- 【未知】没有足够证据证明这些能力经过多用户、长期、完整流程验证。

### 8.8 仍停留在设想中的能力

- 【事实】秋招、春招、提前批、日常等 campaign 模式及自动周计划仍在 PRD。
- 【事实】一周无投递、停滞岗位确认、周复盘等完整任务系统尚未核验到实现。
- 【假设】Obsidian 式岗位知识脉络图是用户提出的方向，当前没有核验到完整图谱交互。
- 【假设】多轮面试总结自动丰富项目和经历知识库的完整闭环尚未被真实案例证明。
- 【事实】用户否定后的轻量返回导航尚未完成新一轮实现。

### 8.9 当前首页向用户传达的产品是什么

- 【事实】当前首页标题是“今天”，核心内容是最近面试节点、推进中的岗位、待补事项、未来日程和 Agent 更新。
- 【判断】它首先传达的是“求职进程与今日行动管理器”，接近带提醒的求职 CRM。
- 【判断】它没有优先展示“为什么这些建议可信”“准备文档如何继承”“面试反馈如何改变经历资料”，因此没有充分传达本轮发现的深层闭环。

### 8.10 首页中心和产品真正中心是否一致

- 【判断】部分一致：首页确实把用户带回真实岗位和下一步行动。
- 【判断】但不完全一致：本轮用户最坚持的是岗位证据、可编辑文档和反馈回流；首页最突出的是状态、数量和待办。
- 【未知】我只能判断本轮参与模块的中心，不能断言整个 Caddie 的唯一真正中心。

### 8.11 哪些页面最能体现产品本体

- 【判断】岗位概览：如果真正展示 JD 要求、个人证据、能力维度、未知和缺口。
- 【判断】准备知识：如果多篇文档可快速定位、编辑并显示来源。
- 【判断】单轮面试页：如果能从原始资料恢复答卷、允许纠正，并把采纳的行动带入下一轮。
- 【判断】三者连起来比首页、看板或聊天框更能体现产品本体。

### 8.12 用户第一次进入时能否理解为什么需要 Caddie

- 【判断】目前不一定。
- 【判断】用户容易理解它能管理岗位和待办，却未必理解它比表格、Obsidian 加通用 AI 多出的价值。
- 【判断】首次体验需要用一个真实岗位展示“JD → 证据 → 准备文档 → 面试答卷 → 确认回流”，而不是先展示功能数量。

### 8.13 哪些交互容易造成误解

- 【判断】求职看板、阶段列、统计数字和提醒会让产品被理解为求职 CRM。
- 【判断】知识书架、文件夹和知识树会让产品被理解为普通知识库或 Obsidian 替代品。
- 【判断】“问 Caddie”、大面积聊天工作区和 Agent 工作台会让产品被理解为套壳聊天机器人。
- 【判断】一次性生成总结、话术或匹配分会让产品被理解为 AI 文案工具。
- 【判断】只有当这些入口共同指向同一岗位事实、同一组文档版本和同一条反馈回流时，误解才会减少。

---

## 9. 最重要的结论

### 1. 我参与的模块里，最有价值的产品判断是什么？

【判断】面试准备不是一段 AI 生成文本，而是岗位知识的“可追溯文档分支”：每轮继承、每轮独立编辑、面试后再把确认反馈带到下一轮。

### 2. 用户做了哪一个我作为 Agent 不会自然做出的决定？

【判断】用户坚持准备必须是多篇文档，甚至可以形成知识树，并把“面试现场快速找到准确位置”放在页面简洁和 AI 自动生成之前。Agent 更自然的做法是生成一份完整总结或一个大编辑器，但那不符合真实使用。

### 3. 哪个能力看起来很炫，但不应该成为 Caddie 的主要亮点？

【判断】聊天式 Agent 工作台、自动生成面试总结和岗位匹配分都很显眼，但不应是主要亮点。它们可以被通用模型替代，也最容易掩盖来源和错误。

### 4. 哪一个真实流程最值得在三分钟 Case Study 中展示？

【判断】选择一个已有岗位：展示 JD 与已确认经历证据的差距；打开上一轮准备文档；复制为本轮独立版本并调整重点；导入面试逐字稿恢复答卷；用户纠正一处识别；采纳一个复盘行动进入下一轮准备。这个流程同时展示上下文、AI判断、人工确认、版本和反馈闭环。

### 5. 关于 Caddie，我最没有资格下结论的部分是什么？

【未知】我最没有资格判断 Caddie 的整体市场定位、真实用户留存、各模块使用频率，以及它是否已经成为一个被验证的完整产品。我参与的是若干页面和局部闭环的重构，不能用这些证据替整个产品发言。
